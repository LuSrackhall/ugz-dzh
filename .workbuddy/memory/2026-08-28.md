# 2026-08-28

## 跨平台打印版渲染差异：最终方案落地（凌晨 01:30 续昨日排查）

- **需求定论（用户明确）**：最终用户 Mac/Windows 都有，需要"**每端各自正确**"（本端生成→本端打印/PDF 输出正确），**不需要跨端像素级一致**。否决：PDF 跨端分发（用户本地生成）、Windows 缩放补偿（长宽比不一致）、双端统一字体（同字体下预览/PDF 也不一致）。
- **Windows 输出实测（2026-06.pdf，GL 库存现金，Windows WPS 导出）**：表格 **19.49×14.65cm** vs Mac 基准 22.28×16.42cm（宽 -12.5%、高 -10.8%）；数据行行距 **17.8pt** vs 设计 18.5pt（25pt×74%，-6%）；纸张 B5 横向一致；装订边基本一致。→ Windows 渲染整体偏小。
- **实现（已编译+测试通过，Mac 无回归）**：
  - 新增 `generator/platform_compensate.go`：`platformCompensate()` 按 `runtime.GOOS=="windows"` 返回 (colScale=1.143, rowScale=1.04)，Mac=1。
  - `print_common.go transformSheet` 4 处：金额列 sub、拆分非金额列 subW、非金额列 w 均 ×colScale（零宽列保持 0）；行高回填 ×rowScale。
  - 原理：transformSheet 是 GL/ML 打印版列宽/行高的唯一入口，补偿集中在此，查看版与 Mac 端不受影响。
- **待办**：Windows 上重新 build + generate → 导出 PDF → 迭代收敛 colScale/rowScale。
- 教训：excelize v2.10.1 无 PDF 导出；LibreOffice 未装；WPS 无 CLI；PDF 生成靠各端 WPS"打印为 PDF"。PDF 测量用 pypdf 解析内容流矢量坐标（pypdf 的 get_contents() 对 Contents 数组返回空，需直接读 page['/Contents'] 遍历流）。

## v0.5.0 发布（01:34）

- 提交：7613e53 fix(print) 平台补偿 + 0505695 chore(memory) 工作日志；tag **v0.5.0** 已移到 HEAD(0505695) 并 push（main + tag force 更新）。
- goreleaser build --clean 全平台构建成功（linux/darwin/windows × amd64/arm64）；**手动打包** dist/ugz-dzh_v0.5.0_windows_amd64.zip / _arm64.zip（goreleaser build 不产归档，GitHub release 需 GITHUB_TOKEN 跑 goreleaser release，未做）。
- 验证：ledger.exe 为 PE32+ x64；GOOS=windows 编译正常。
- 待办：Windows 上跑 v0.5.0 → generate → 导出 PDF → 迭代 colScale/rowScale 收敛。

## v0.5.1 回退发布（01:52）

- v0.5.0 补偿（列宽×1.143/行高×1.04）在 Windows 实测**页面整体溢出**：Mac 设计表格 22.28×16.42cm 已占满页面可用（22.27×16.41cm），放大必撑爆。
- **结论：Windows 不能"拉向 Mac 尺寸"，必须"适配 Windows 页面"重新标定布局参数**（适配可用区域 + 宽高比 1.357 + 装订边）。
- 回退 commit 280105a + e06649f（memory），tag v0.5.1（指向 e06649f），全平台构建 + windows amd64/arm64 zip 已出。
- platform_compensate.go 保留框架，windows 系数暂 1.0（不溢出可用），TODO 待 Windows 端重新标定。

## Windows 系数标定收敛（02:44）

- 用户肉眼观察迭代收敛：列宽 ×1.1075、行高 ×0.992（Windows 端；Mac 恒 1.0）。
- 迭代轨迹：1.1/1.01 → 1.12/1.009 → 1.115/1.007 → 1.112/1.005 → 1.11/1.002 → 1.105/1.0 → 1.107/0.98 → 1.108/0.99 → 1.1073/0.995 → 1.1075/0.993 → 1.1076/0.991 → 1.1075/0.992（收敛）。
- 工作流：gen-win-test.sh（Mac 生成 Windows 版）→ 发 Windows 肉眼观察 → 调 platform_compensate.go 系数 → 迭代。

## v0.6.0 发布（02:45）

- tag v0.6.0（指向 60e2039）已 push；goreleaser build 全平台成功；windows amd64/arm64 + darwin arm64 zip 已打。
- 包含：--platform 参数（auto/mac/windows）、gen-win-test.sh、Windows 补偿系数标定（列宽1.1075/行高0.992）。

## print-config.json 配置化（03:20）

- 用户放弃 Arimo 三端统一（改主意），改为 **CLI 配置文件**：generate --config print-config.json，可配平台补偿系数 + 分区域字体，免发版调参。
- 实现：generator/print_config.go（map[string]any 解析中文键——**Go encoding/json 不支持中文 struct tag（Go 1.17+），必须手动 map 取中文键**）、platform_compensate 系数从配置读、print_font/print_gl/print_ml 字体分区域从配置读（基准/数字/标题/默认）、TransformToPrint SetDefaultFont(基准)、cmd --config 参数、example/print-config.json 示例。测试 TestLoadPrintConfig 通过，端到端验证 1.2 vs 1.1075 列宽精确匹配。
- 提交 76c5bdd（v0.6.0 之后，未发版）。

## print-config.json 重构：按平台分组 + 英文键（03:30）

- 用户要求：配置**按平台各自设置**（系数+字体独立）、键改**英文**（避开 Go json 中文 tag 坑），文档解释清楚。
- 重构：printCfg 结构 → `platforms.{windows,mac}.{colScale,rowScale,fonts.{normal,digit,title,default}}`；英文键直接用 struct tag 解析（不再 map hack）；`currentPlatform()/platformConfig()/currentFonts()` 按目标平台取配置。引用处（platform_compensate/print_font/print_gl/print_ml/print_transform）改用 currentFonts()/platformConfig()。
- 新增 docs/print-config.md（字段说明+用法+示例），example/print-config.json 更新英文键。
- 测试：TestLoadPrintConfig（部分覆盖默认保留）+ TestCurrentFontsByPlatform；端到端验证英文键 colScale 1.2 → 列宽 178.7 ✓。

## 三处体现核对（03:25）：配置功能文档化完成

- CLI 帮助：--config/--platform 描述 ✓（已在）
- 项目文档：docs/print-config.md + docs/print-config.example.json ✓（已在）
- **skill 文档补全**：embedded/ledger-accounting/references/commands.md（generate 参数补 -p/--config 说明）+ SKILL.md（命令表 + 打印章节补配置说明）；go:embed 机制，改源即可 build 自动嵌入。

## v0.7.0 发布（03:23，用户将去 Windows 实测）

- tag v0.7.0（96bb7ba）已 push；goreleaser build 全平台成功；windows amd64/arm64 zip 已打。
- 包含（v0.6.0 之后）：print-config.json 配置化（平台补偿系数 + 分区域字体，按平台分组英文键）+ docs/print-config.md + skill 文档补全。
- 用户将用 v0.7.0 Windows 版实测打印版；若需调系数，改 print-config.json 免发版。

## 配置排查：generate 打印"打印版配置"摘要（03:40）

- 用户反馈 Windows 上配置不起作用。自查：v0.7.0 windows 二进制含配置功能（strings 验证 --config/colScale 均在）；Mac 端到端验证配置生效。
- **发现时序 bug**：PrintPlatform 原在 TransformToPrint 前才设置（cmd/generate.go:220），而 LoadPrintConfig 在前 → 配置加载/摘要基于错误平台。修复：PrintPlatform 提前到 LoadPrintConfig 之前（cmd/generate.go:39）。
- 新增：generator.CurrentConfigSummary() + generate 打印"打印版配置: 默认/已加载 → 平台=.. 列宽系数=.. 行高系数=.. 字体=.."，Windows 上一跑即知配置是否生效。
- 用户侧最可能原因（待确认）：① 没显式传 --config（CLI 不会自动找当前目录配置）② 配置用了中文键（英文键重构后被静默忽略）③ 记事本 ANSI/GBK 编码或 UTF-8 BOM 导致解析失败 ④ 看错文件（补偿只作用于 print/ 打印版，需重新 generate）。

## 配置防呆 + 三处文档补强（03:47，用户指出 agent 用 skill 会犯错）

- 用户核心观点：用 CLI 的是 agent（经 skill），skill 没写清楚=skill 的错。
- **代码防呆**（generator/print_config.go）：① UTF-8 BOM 自动容忍（bytes.TrimPrefix）② 中文键检测→明确报错（json 对未知字段静默忽略→旧格式配置不生效，现在直接报"中文字段名…请改用英文键"）③ 解析错误提示补"UTF-8 无 BOM、无注释/尾逗号"。
- **CLI help**：--config 描述补全（必须显式传参/英文键/UTF-8/只作用于打印版）。
- **docs/print-config.md**：新增"⚠️ 常见错误（agent 操作必读）"表（4 种错误+后果+正确做法）+ 自检方法。
- **skill 文档**：commands.md 的 --config 补 4 条要点；SKILL.md 打印章节补防呆提示。
- 验证：中文键→明确报错 ✓；BOM→正常加载 ✓；英文键→生效 ✓。
- 提交 2c90a9f 后新增改动未提交。

## v0.7.1 发布（03:42）

- tag v0.7.1（ae3800b）已 push；goreleaser build 全平台成功；windows amd64/arm64 zip 已打。
- 包含（v0.7.0 之后）：PrintPlatform 时序修复 + 配置摘要打印 + 配置防呆（中文键报错/BOM 容忍）+ 三处文档补强。

## 配置自动发现（03:55，Windows agent 写后处理脚本事件）

- 用户反馈：Windows 上的 agent 因"配置不生效"写了后处理脚本绕过 → 用户批评"能在 CLI 解决的为什么让人家写脚本"。
- **根因坐实**：`.agents/skills/ledger-accounting/` 是 install-skill 安装副本（gitignore，不进 git），落后于 embedded 源 → Windows agent 用的旧 skill 不知道 --config/--platform → 写脚本。**每台机器须重跑 `ledger install-skill` 才拿到新版 skill**。
- **CLI 改进（治本）**：`generate` 未传 --config 时**自动发现当前工作目录 print-config.json**（有则生效，无则默认；显式 --config 优先）——配置放运行目录即生效，agent 无需知道传参。打印"已自动发现当前目录"。
- 文档同步：docs/print-config.md 常见错误表第1条改为"配置放非当前目录"；skill commands.md/SKILL.md 补自动发现说明；.agents/skills 副本 rsync 同步。
- 验证：①cwd有配置→自动发现1.25生效 ②无→默认1.1075 ③--config显式优先1.2 全通过。

## init 建立完整管理体系（04:05，用户要求"CLI 默认管理体系架构"）

- 用户要求：① init 时就把凭证输入目录和需要的 json 建好，CLI 有默认管理体系架构；② embedded 是唯一权威源，win 重跑 install-skill 即最新（install_skill.go 每次 RemoveAll+全量重写，机制已确认）。
- **init 扩展**（cmd/init.go）：一次性建立——vouchers/（凭证输入目录+README 命名说明）、print-config.json（打印版配置模板，generate 自动发现）、README.md（体系说明）、{year}/{year}.json。幂等：模板/README/vouchers 已存在保留（仅补缺失）；年份 JSON 已存在报错。
- **自动发现扩展**（cmd/generate.go）：未传 --config 时依次查 当前工作目录 → 输出根目录 的 print-config.json。
- skill 文档同步（init 章节、SKILL.md 建账/每月记账章节），.agents/skills 副本 rsync。
- 验证：init 建体系 ✓ / 输出根目录自动发现 ✓ / windows 1.1075 ✓ / 幂等保留用户配置 9.99 ✓ / 全量测试（含 e2e）全过。

## skill 包自包含化（04:12，用户指出开发/生产环境不分）

- **教训（用户点破）**：Windows 是生产环境（只有 ledger.exe + 安装的 skill），而 skill 文档引用了源码项目里的 docs/print-config.md、scripts/gen-win-test.sh、scripts/rebuild.sh、docs/print-config.example.json——**生产环境找不到这些文件** → agent 只能写脚本绕过。
- **修复：skill 包自包含**（embedded/ledger-accounting/ 新增 4 文件）：
  - references/print-config.md（= docs/print-config.md 副本，内部引用改包内路径）
  - examples/print-config.example.json（模板）
  - scripts/gen-win-test.sh（副本，头部注明"开发环境辅助，生产用 --platform windows + print-config.json"）
  - scripts/rebuild.sh（副本，注明"随 skill 分发"）
- 引用路径批量替换：docs/print-config.md → references/print-config.md；docs/print-config.example.json → examples/...；SKILL.md"项目根见 workspace" → "本 skill 自包含"。
- CLI help：--config 描述去掉 docs/ 引用，改为"ledger init 自动生成模板 + 英文键格式要点"。
- docs/print-config.md 失效引用修正（example/ 被 gitignore → docs/print-config.example.json）。
- 验证：包内无残留 docs/example/（源码）引用，所有引用文件在包内；.agents/skills 同步；全量测试过。

## 用户画像明确 + doctor 自检 + 根命令引导（04:20）

- **用户画像（用户明确）**：用户=agent 及使用 agent 的人；生成端=agent 用 skill 驱动 CLI。产品要求：agent 首次接触能自助理解体系、排障能自助定位。
- **新增 `ledger doctor [-o <输出根>]`**（cmd/doctor.go）：一次查清 程序版本、skill 安装且自包含（references/print-config.md 存在=新版）、print-config.json 自动发现（cwd→输出根）、账本 {year}/{year}.json（严格形态，排除 dist/docs）。输出 [OK]/[WARN]/[FAIL] + 结论 + 修复指引。
- **根命令引导**（cmd/root.go）：无参数运行 ledger 打印 agent 使用引导（install-skill → init → 凭证 → generate + doctor 排障）。
- skill 文档（commands.md doctor 章节、SKILL.md 命令表）同步；.agents/skills 副本同步。
- 修复：doctor 账本检查误报 dist/docs JSON → 仅认 {year}/{year}.json 形态。

## 版本管理与 skill 版本联动（04:30，用户批评工程化）

- **用户批评**：① 用户只向 agent 提问（老板不懂会计）② CLI 无版本管理（--version 硬编码 1.0.0，与 tag 脱节——.goreleaser.yml 无 ldflags，所有发布包版本都是假的）③ CLI 更新=skill 也要更新，无联动。
- **修复**：
  - cmd/root.go：`var version = "dev"`（ldflags 注入点）；.goreleaser.yml 加 `-X ledger/cmd.version={{.Version}}` → 发布包显示真实 tag 版本。
  - **skill 版本联动**：install-skill 写入 `<skillDir>/VERSION` = 当前 ledger 版本；doctor 新增"版本匹配"检查（CLI vs skill VERSION，不匹配 [FAIL] 提示重跑 install-skill；本地 dev 跳过）。
  - SKILL.md frontmatter 加 version 字段 + 版本联动/用户画像说明；commands.md doctor/install-skill 章节补版本联动。
- 验证：dev 构建 --version=dev；ldflags 注入 v0.7.2-test 生效；doctor 版本不匹配 FAIL/匹配 OK；全量测试过。
- **教训（用户画像）**：最终用户=老板（只提问），agent=专属会计（读 skill 驱动 CLI）。文档不是给用户读的，是给 agent 的操作手册；CLI 输出要 agent 可解析、错误带修复指引。

## v0.7.2 发布（04:08）

- tag v0.7.2（9d3efea）已 push；goreleaser build 全平台成功。
- **版本注入首次生效**：Windows 二进制 strings 验证真实版本 v0.7.2（之前所有发布包是假的 1.0.0）。doctor/install-skill/VERSION 特征 26 处。
- zip：windows amd64/arm64 + darwin arm64。
- 包含（v0.7.1 之后 7 commit）：防呆（中文键报错/BOM）、自动发现配置、init 完整管理体系、skill 自包含化、doctor 环境自检+版本匹配、根命令 agent 引导、版本注入+skill VERSION 联动。

## GL/ML 分账本配置（04:40，用户问"gl,ml用的是一套吗，能分开吗"）

- 之前 GL/ML 共用 platforms.<平台> 一套系数+字体。现支持分账本覆盖：
  platforms.<平台>.{gl,ml}.{colScale,rowScale,fonts}，未填字段回退平台级；"gl":{} 视为未配置。
- 实现：print_config.go（SheetConfig/PlatformConfig.GL/ML/printSheetType 上下文/
  sheetConfig()/sheetCompensate()/currentFonts 感知账本类型/mergeFonts/LoadPrintConfig 合并）；
  print_transform.go 遍历时设 printSheetType；print_common.go 改 sheetCompensate；
  platform_compensate.go 删除冗余 platformCompensate。
- 验证：单元测试 4 个全过；端到端 GL colScale=1.3 → GL 列宽 175.4 vs ML 164.9（独立生效）。
- docs/print-config.md + example 加 gl/ml；skill 副本同步。

## Windows GL 默认系数标定（04:46，用户给值）

- 用户标定：Windows 端 **GL 默认 colScale=1.13595、rowScale=0.99495**（ML 保持平台级 1.1075/0.992）。
- 修改：defaultPrintConfig() windows 平台加 GL:&SheetConfig{1.13595, 0.99495}；CurrentConfigSummary 摘要加 GL/ML 覆盖显示（回退后生效值）；init printConfigTemplate 与 docs example 同步（windows.gl=1.13595/0.99495）；docs 默认值说明更新。
- 验证：摘要显示 GL(列宽1.1360 行高0.9950)；GL 列宽 175.4(1.3)→153.3(1.13595) 比例 0.8738 精确匹配；ML 保持 164.9。
