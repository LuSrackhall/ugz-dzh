# 2026-09-01 工作日志

## 用户画像澄清（17:50，用户明确）
- **CLI 与技能的主要用户是 agent 智能体**，不是人类；人类是次要用户。技能为 agent 服务，维护动作（git 纳管、升级）由 agent 按技能指引驱动。
- **技能安装=仓库级**：技能仅装 CLI 所在目录（.agents/skills/），用户级（home 下）残留是历史/误操作产物，应检测清理。

## v0.8.1 发布（18:15，用户"现在实施"批准）

- **需求**：install-skill 检测用户级技能残留（~/.workbuddy、~/.claude、~/.cursor、~/.codex、~/.gemini、~/.windsurf、~/.zed 等），提示移除（推荐仅仓库级）。
- **实现**：cmd/install_skill.go 新增 userLevelAgentDirs + findUserLevelSkills + checkUserLevelRemnants；交互终端询问默认移除、非终端默认移除、--keep-user-level 保留。单测 cmd/install_skill_test.go 3 用例。
- **技能文档**：commands.md/SKILL.md 补 git 纳管指引（git add -f .agents/skills/ && commit，-f 因 .gitignore 忽略安装产物）。
- **发版流程教训**：项目已配 GitHub Actions release.yml——**push v* tag 即自动 goreleaser 发布**（github-actions[bot] 建 release），勿手动 gh release create（会 422 撞车）。本次手动 create 撞车后改用 CI 产物（下载 windows zip 验证 version 0.8.1 注入 OK）。
- **goreleaser 坑**：dirty 工作树（未跟踪 .workbuddy/memory/*.md）会 fail；tag 必须指向 HEAD（先提交 memory 再打 tag，或 tag 后补提交需重建 tag）。

## v0.8.0 发布（用户"发个新版本吧"，16:06 完成）

- **版本号**：用户选定 v0.8.0（非 v0.7.20）。
- **变更基线**：v0.7.19..HEAD 共 15 提交，核心 3 个功能：① 打印版自动写多区域打印区域（导出 PDF 无空白页且正反配对，53562fb）② subjects/opening 批量导入命令（建账审核表 CSV 一次导入科目与期初，试算平衡强制闸门 + --dry-run + check --vs 验收比对，be5aa53）③ 科目顺序接线（账页 Sheet 按 全局设置.科目顺序 归位 GL→合并→ML 分区块，哨兵前插法，打印版自动跟随，4cde678）。
- **发现**：v0.7.19 tag 上 SKILL.md version 仍为 0.7.18（v0.7.19 发版漏同步）→ 本次直接改 0.8.0 并 rsync .agents 副本（VERSION 文件保留不覆盖）。
- **发版流程（完整执行）**：① 定版本号 0.8.0 ② 改 embedded/ledger-accounting/SKILL.md version + 同步 .agents ③ go test ./... 全绿 + test-e2e.sh 全量（9 个月生成 OK）④ git tag v0.8.0 + push main/tag ⑤ goreleaser release --clean --skip=publish（goreleaser v2 用 --skip=publish 而非 --skip-publish）⑥ 校验 darwin/win 产物 version 注入 + embed skill version ⑦ gh release create v0.8.0 上传 6 平台包 + checksums.txt ⑧ 发布成功 https://github.com/LuSrackhall/ugz-dzh/releases/tag/v0.8.0
- **Windows 端待办（版本联动）**：新 exe 替换 + 重跑 install-skill（VERSION=v0.8.0）+ doctor 自检。
- **教训**：v0.7.19 漏同步 SKILL.md version 的教训——每次发版必须按强制顺序执行（先定版本号→改 SKILL.md version→编译→tag→构建），本次已落实。
