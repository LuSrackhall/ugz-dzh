# 2026-09-06 工作日志

## 记忆跨 agent 接入：CLAUDE.md 改名 AGENTS.md + 一行垫片（ZCode 会话）

### 用户诉求
- 项目记忆在 `.workbuddy/memory/`，但只有 WorkBuddy 读写；其它 agent（ZCode / Claude Code / Cursor）不知道它存在，既不读也不维护。用户只关心 memory 跨 agent 可用，不涉及其它配置。

### 方案与关键查证
- 用户拍板：`CLAUDE.md` 改名 `AGENTS.md`，加入记忆约定节。
- 查证（官方文档 code.claude.com/docs/en/memory）：**Claude Code 不原生读 AGENTS.md**（原话 "Claude Code reads CLAUDE.md, not AGENTS.md"）→ 改名后必须保留一行垫片 `CLAUDE.md`（内容仅 `@AGENTS.md`）。官方推荐 @import 而非软链接——Windows 软链有权限坑，且本仓库惯例是软链接不进 git（见 .gitignore 技能接入层注释）。
- 仓库引用清点：仅 README.md:251 一处活引用（已改为 AGENTS.md）；openspec 归档里的 CLAUDE.md 字样属历史记录，不动。

### 落地
- `git mv CLAUDE.md AGENTS.md`（历史保留），新增「项目记忆（跨 agent 共享）」节：开工前读 `.workbuddy/memory/MEMORY.md`、完工后更新 MEMORY.md + 当天日期日志、禁止另存副本分叉。
- 生效路径：ZCode / Codex / Cursor 直读 AGENTS.md；Claude Code 经垫片 @import 同源；WorkBuddy 照旧 `.workbuddy/`；记忆本体 `.workbuddy/memory/`（git 已跟踪）不变。

### 待办
- AGENTS.md「提交规范」示例仍是模板残留（"朝向系统 / 盾牌血条"，与账务项目无关），待用户决定是否替换为本项目示例。
