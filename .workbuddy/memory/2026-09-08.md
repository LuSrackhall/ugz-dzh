# 2026-09-08 工作日志

## 明细科目独立账页：GL 式 → ML 式返工（用户两次纠偏）

### 两次纠偏
1. **"看不到独立明细页"**：根因 = 功能源码从未编译进项目根 `ledger` 二进制（旧 17:35 版），我昨天验证走测试内临时编译、没更新交付物。已重建（教训：**改动完成后必须 `go build -o ledger .` 更新用户实际运行的二进制**）。
2. **"用的还是多科目明细账的账页？把总分类账改个名就行了？"**：用户明确设计模型——**账页样式只有两种纸：GL 三栏式、ML 多栏式**。GL=总分类账默认分类（合并需配置）；ML=明细账默认合并（分离需配置）。明细账独立页 = **ML 家族的分离形态**，页子必须是 ML 那张多栏式账页（四行表头/明细列区域/左右双面），明细列自然为空——不是"GL 引擎 + 改标题"。我此前把"明细列空"错误理解成"改用三栏式"，返工切换引擎。

### ML 式实现（返工后）
- `appendToMLSheet` 拆 `appendToMLSheetBody(sheet, accountLabel, entries, detailIdx, initial)` 共享本体（3 处 writeMLPageHeader 的 general→accountLabel）；`ensureMLStyleDetailSheet`（Paper1 占位页 + writeMLPageHeader(1,0,0,account,false,true)，同 ensureMLSheet 新建分支）+ `appendToMLDetailSheet`（detailIdx 空 map → 明细列全空）。
- `WriteMLMonthClosings` 改 targets 序列：合并 ML 页（照旧）+ 独立明细页（detailGroups 按叶子全路径分组，label=全路径）——月结循环体零搬动；明细列断言全空路径天然退化（details[i]=="" 跳过 getDetailPrev*/列写入）。
- monthly_close.go 撤 GL 侧独立页月结轮（月结归 ML 家族专职）；workbook.go setAllSheetPageLayout 明细页改跳过（归 setMLSheetPageLayout）；ml_page_layout.go 前缀扩 `明细账-`；print_transform 明细页 case 走 transformMLSheet（11/10 列拆位）；sheet_order 区块顺序改 GL→合并→**ML→明细账**（分离形态殿后，用户模型）。
- 仅期初无分录路径：建页写期初行后手动 padMLPage（无月结键不会补结构过次页）。
- **关键布局事实坑：ML 的 BindingLeftCols=1（非 GL 的 2）**——ml_layout.go 字段注释 "// 2" 过时误导（已修正注释）；GetRows 索引：摘要=1+4=5、余额=1+8=9、借方=6。单测/e2e 断言按动态 lay.BindingLeftCols 或 5/9。
- ML 期初标签语义：isNew && initial≠0 → "期初余额"（无 GL 的 1 月"上年结转"区分）；isNew && initial=0 → "承前页"。
- 测试：generator 7 个单测重写为 ML 式断言 + e2e 列位/排序更新；全量 -count=1 绿 + e2e 脚本绿 + 双目录同步 + embed 守护绿。
- 坑：Edit 锚点截断在行中间（`targets =`）造成残留文本——old_string 必须含完整行；3 月是季末（3/6/9/12），测试断言别想当然。
