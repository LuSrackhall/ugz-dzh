# 2026-09-08 工作日志

## 明细科目独立账页：GL 式 → ML 式返工（用户两次纠偏）

### 两次纠偏
1. **"看不到独立明细页"**：根因 = 功能源码从未编译进项目根 `ledger` 二进制（旧 17:35 版），我昨天验证走测试内临时编译、没更新交付物。已重建（教训：**改动完成后必须 `go build -o ledger .` 更新用户实际运行的二进制**）。
2. **"用的还是多科目明细账的账页？把总分类账改个名就行了？"**：用户明确设计模型——**账页样式只有两种纸：GL 三栏式、ML 多栏式**。GL=总分类账默认分类（合并需配置）；ML=明细账默认合并（分离需配置）。明细账独立页 = **ML 家族的分离形态**，页子必须是 ML 那张多栏式账页（四行表头/明细列区域/左右双面），明细列自然为空——不是"GL 引擎 + 改标题"。我此前把"明细列空"错误理解成"改用三栏式"，返工切换引擎。

### ML 式实现（返工后）
- `appendToMLSheet` 拆 `appendToMLSheetBody(sheet, accountLabel, entries, detailIdx, initial)` 共享本体（3 处 writeMLPageHeader 的 general→accountLabel）；`ensureMLStyleDetailSheet`（Paper1 占位页 + writeMLPageHeader(1,0,0,account,false,true)，同 ensureMLSheet 新建分支）+ `appendToMLDetailSheet`（detailIdx 空 map → 明细列全空）。
- `WriteMLMonthClosings` 改 targets 序列：合并 ML 页（照旧）+ 独立明细页（detailGroups 按叶子全路径分组，label=全路径）——月结循环体零搬动；明细列断言全空路径天然退化（details[i]=="" 跳过 getDetailPrev*/列写入）。
- monthly_close.go 撤 GL 侧独立页月结轮（月结归 ML 家族专职）；workbook.go setAllSheetPageLayout 明细页改跳过（归 setMLSheetPageLayout）；ml_page_layout.go 前缀扩 `明细账-`；print_transform 明细页 case 走 transformMLSheet（11/10 列拆位）；sheet_order 区块顺序改 GL→合并→**ML→明细账**（分离形态殿后，用户模型）。
- 仅期初无分录路径：建页写期初行后手动 padMLPage（无月结键不会补结构过次页）。
- **关键布局事实坑：ML 的 BindingLeftCols=1（非 GL 的 2）**——ml_layout.go 字段注释 "// 2" 过时误导（已修正注释）；GetRows 索引：摘要=1+4=5、余额=1+8=9、借方=6。单测/e2e 断言按动态 lay.BindingLeftCols 或 5/9。
- ML 期初标签语义：isNew && initial≠0 → "期初余额"（无 GL 的 1 月"上年结转"区分）；isNew && initial=0 → "承前页"。
- 测试：generator 7 个单测重写为 ML 式断言 + e2e 列位/排序更新；全量 -count=1 绿 + e2e 脚本绿 + 双目录同步 + embed 守护绿。
- 坑：Edit 锚点截断在行中间（`targets =`）造成残留文本——old_string 必须含完整行；3 月是季末（3/6/9/12），测试断言别想当然。

## 边框错乱根因确认（用户"边框线样式乱"线索 → 真实 bug）

- 用户"既然没问题就实现吧，做好 git 管理"——提交前对用户指出的**边框**做最后一轮对比（前轮只比了合并区/行高/列宽，漏了边框本身），抓到真 bug：
- **根因：昨天在 finalizeAllGLSheets（GL 页末补齐）扩的 `明细账-` 前缀没随 ML 式返工撤掉**——GL 坐标系页末逻辑往 ML 式独立页写 GL 式过次页标签（行 27 c7 = GL 摘要列位，ML 摘要在 c6）+ 摘要列样式 → 边框/内容错乱（独立页 871 边框格 vs 合并页 598）。
- 修复：finalizeAllGLSheets 两处循环恢复仅扫 `总分类账-` 前缀（独立页页末补齐归 ML 家族：月结末尾/仅期初 padMLPage）。修复后独立页 vs 合并 ML 页边框**逐项归零**（598=598、逐行一致、样式分布一致、行 27 残留清除）。
- 方法论沉淀：**对比验证要覆盖用户指出的具体维度**（用户说边框就比边框，不能只比结构）；写路径污染用"坐标指纹"定位（c7=GL 摘要位 vs c6=ML 摘要位，一眼识别是哪个引擎写的）。
- git 管理（用户要求可回溯）：15e9236 feat 功能 / bbb2bcf docs 技能+openspec / 6bff43e docs 记忆，工作树干净；push 待批准（41f95f8 起共 4 个本地 commit 搭车）。

## 发版 v0.9.5 + 标准 e2e 纳入新功能（用户要求）

- 用户指出标准 e2e（scripts/test-e2e.sh 演示账套）没覆盖新功能 → 以 **公益支出-补助费用** 为准配置 `明细账独立科目`（选它因无 OCR 变体；"公共基础设施维护提升"存在带空格变体两个键）。脚本 ML_SUPPRESS 旁加 DETAIL_STANDALONE 写入 init 后的 JSON，year-close cp 配置自动跨年延续。
- 真实账套实证（out/2026/2026-06.xlsx）：独立页 index 102（ML 区块后）、9 个月 120 行、3 次真实翻页、期末 530,843（2025-12 的 93,393 跨年续写）、合并 ML 列头无"补助费用"、GL 叶子页照常。
- 发版序列：全量测试绿 → CHANGELOG v0.9.5 人话节 → SKILL 双目录 bump 0.9.5 → goreleaser snapshot 构建过（19s 双平台）→ 提交 → push main+tag（用户"发版吧"= 显式批准）→ watch CI → verify Latest → release edit 贴人话节。

### v0.9.5 发版完成
- push main+tag 终态 = 77da78a（本地 tag 已存在报 fatal 但远程终态正确，以 ls-remote 校验为准——与 v0.9.4 插曲同款教训重演）；CI 三条全绿（CI/Build/Release）；release v0.9.5 已是 **Latest**，正文 = 指针行 + 人话节 + 自动清单（gh release edit 拼接）。
- 待用户动作：Windows 端 exe 替换 → install-skill → doctor 自检（CLI↔skill 0.9.5 联动）。

## v0.9.6：分离明细账科目语义校准（用户第三次纠偏）

- 用户模型澄清：**配置应像"合并总账科目"一样按总账科目名**——配 `["公益支出"]` → 其下所有明细各自分离成页（实测 9 张），不是逐明细配置；且**分离页与合并页互不冲突共存**（对仗 GL 叶子页与合并页共存）——v0.9.5 的"列收缩"撤除，合并 ML 明细列照常保留。
- 字段更名：`明细账独立科目`（v0.9.5，作废）→ **`分离明细账科目`**（v0.9.6）；代码 DetailStandalone→DetailSplit；双匹配（总账名/叶子全路径，对仗"总分类账忽略科目"先例）；**无明细段的总账直记路径不命中**（分离页只按叶子建）。
- 改动：detailSplit/detailSplitSet（→ []string）、CollectChangedSheets 改判定器签名 func(string)bool、ml_sheet 撤列排除+撤存量冲突报错、父级校验保留、孤儿清理走 detailSplit。
- 演示账套实证：9 明细全部分离、合并列保留、补助费用期末 530843=JSON 链。测试重写（TestDetailSplitMatch/KeepsSplitDetailColumns/GenerateWorkbookDetailSplit + e2e 5.5 段 + 守门测试）。
- 教训：**对仗设计要问到"配置粒度"层**——用户说"分离需配置"时我配到了叶子粒度，他的对仗锚点是"合并总账科目"（总账粒度）；"互不冲突"=不删减既有视图，不是"互不影响"的同义词。
- 状态：全量测试绿 + e2e PASS + goreleaser snapshot 过 + 文档/CHANGELOG v0.9.6 节就绪；**push tag v0.9.6 待用户批准**（commit 已本地）。

### v0.9.6 发版完成（用户批准）
- push main+tag 终态 = 4f7b403（同款双执行插曲：本地 tag 重创建报 fatal 但远程终态正确，ls-remote 校验为准）；CI 三条全绿；release v0.9.6 = **Latest**，正文 = 指针行 + v0.9.6 人话节 + 自动清单。
- 待用户动作：Windows 端 exe 替换 → install-skill → doctor（0.9.6 联动）；若按 v0.9.5 配过"明细账独立科目"，键名改"分离明细账科目"。
