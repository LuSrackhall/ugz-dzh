# 2026-09-09 工作日志

## v0.9.9：明细列净额按科目方向定向——红字语义修正（用户发起严肃审计）

- **用户问**：ML 明细列什么时候用红字？跟科目方向有关吗？还是只要贷就一定是负？
- **审计结论**：明细列口径原为统一"净额=借−贷"，与科目方向无关；打印版"负数=红字"（H2）组合下——借方科目碰巧全对（贷冲减→红字），**贷方科目（收入/负债/权益类）整列颠倒**（正常贷发生→红字、冲减/红字冲销→蓝字）。红字凭证（带符号借贷，H2 四格式）在贷方科目同样颠倒。
- **手工账规范**：多栏账分析栏以科目基准方向为正，反向发生额红笔（无负号）。打印版"负数=红字"机制本身正确，缺的是净额未按方向定向。
- **修复**：detailColNet(general, debit, credit)——detailCreditBased(general)（树叶子属性优先/官方推断兜底，sync.Once 缓存）为贷则取负。应用点全链：分录行、writeMLPageBreakRow/writeMLCarryForwardRow（签名+general）、writeMLClosingRow（签名+general）、lastBreakDetailTotals（读显示值须**逆向还原**原始口径防二次翻转）。qt/ytd 的 prev 净额已是原始口径塞 debit/credit 分开 ✓。
- **自查坑 1**：0 期初分支改造时 row 赋值挪进 if → 零值行号分录丢失（同昨天 v0.9.8 的坑——**条件化写行时 row 推进必须留在 if 外**）。
- **自查坑 2**：测试期望把"累计净额"当"单笔净额"算（明细列每行=该笔净额）。
- 演示账套公益支出为借方 → 行为不变零回归（e2e 5.5 PASS）。文档：workflows.md 新增"金额分析列的符号与红字规则"节 + commands.md 产物行，双目录同步；CHANGELOG v0.9.9（贷方向科目符号翻转+零迁移+铁律一说明）；SKILL bump 0.9.9。
- 状态：全量测试绿、构建过、commit 本地，发版待批准。
## mac-win-common 模板更新：win 端金额数字字体 MV Boli（用户指示）

- 技能模板库 templates/print-config.mac-win-common.json：platforms.windows.fonts.digit Noteworthy → **MV Boli**（Windows 自带手写风格字体，用户标定偏好）；_comment 记录变更日期；SKILL.md 模板清单表同步。
- 双目录同步 + embed 守护绿 + JSON 合法性验证。此前回答"只有 windows/mac 两个平台键"漏了技能模板库（templates/ 下 3 份预设：win-standard/mac-standard/mac-win-common，config list/apply 可用）——已向用户更正。
## mac-win-common 模板更新：win 端金额数字字体 MV Boli（用户指示）

- 技能模板库 templates/print-config.mac-win-common.json：platforms.windows.fonts.digit Noteworthy → **MV Boli**（Windows 自带手写风格字体，用户标定偏好）；_comment 记录变更日期；SKILL.md 模板清单表同步。
- 双目录同步 + embed 守护绿 + JSON 合法性验证。此前回答"只有 windows/mac 两个平台键"漏了技能模板库（templates/ 下 3 份预设：win-standard/mac-standard/mac-win-common，config list/apply 可用）——已向用户更正。

### v0.9.9 发版完成（用户批准）
- push main+tag 终态 = 5306607（含明细列方向修正 + mac-win-common 模板 MV Boli 更新）；CI Release success；v0.9.9 = **Latest**，正文已贴人话节。
- 待用户动作：Windows 端 exe → install-skill → doctor（0.9.9 联动）；有收入/负债/权益类多科目明细账且在意呈现口径的账套可 -f 重建历史月。

## ledgerOnly：打印版仅含账页开关（用户需求：仅账页 PDF）

- 用户问 PDF 能力边界 → 结论：系统不产 PDF，PDF=打印版 xlsx 的 WPS/Excel 导出产物；账页（GL/ML/分离）有位格精调，日记账/报表原样在打印版（无精调）。
- 用户需求"仅账页 PDF" → print-config.json 顶层 `ledgerOnly`（默认 false 零回归）：TransformToPrint 尾部 removeNonLedgerSheets 移除非账页 Sheet（isLedgerSheet 复用 print_font.go——**新增时重复定义了同名函数，编译前 grep 避免冲突**）；删除后 SetActiveSheet(0)。
- 实测：demo 打印版 7 张纯账页（含分离页）✓。**排查插曲**：demo 分离页"丢失"实为 demo JSON 历次重建时分离配置字段整个丢失（历次重建覆盖）——全链单测（TransformToPrint(viewPath, printPath) 双参数）先证明代码正确，再查数据。
- 测试 4 个（removeNonLedgerSheets/LoadPrintConfig ledgerOnly/Template 字段/全链保留分离页）。文档 docs/print-config.md 新增 ledgerOnly 节。
- 环境坑：Bash 会话 PATH 丢 go（/usr/local/go/bin）——export PATH 恢复。
- 状态：全量绿 + 实测过 + commit 本地，**发版号待定（v0.10.0 建议）待批准**。

## v0.10.0：ledgerOnly 开关 + 废弃字段显式告警（用户批准随防呆一起发）

- ledgerOnly 已实现（见上节），追加防呆：balance.DeprecatedFieldWarnings(data)——LoadConfig 内扫描全局设置已知废弃键（明细账独立科目→分离明细账科目，v0.9.6），命中即 stderr 告警提示迁移。**废弃键清单是 map，未来改名直接追加**。
- 测试：TestDeprecatedFieldWarnings（旧键告警含新键名/版本/新键不告警/无字段不告警）。CHANGELOG v0.10.0 双能力人话节；SKILL bump 0.10.0。

## v0.10.0：ledgerOnly 开关 + 废弃字段显式告警（用户批准随防呆一起发）

- ledgerOnly 已实现（见上节），追加防呆：balance.DeprecatedFieldWarnings(data)——LoadConfig 内扫描全局设置已知废弃键（明细账独立科目→分离明细账科目，v0.9.6），命中即 stderr 告警提示迁移。**废弃键清单是 map，未来改名直接追加**。
- 测试：TestDeprecatedFieldWarnings（旧键告警含新键名/版本/新键不告警/无字段不告警）。CHANGELOG v0.10.0 双能力人话节；SKILL bump 0.10.0。
