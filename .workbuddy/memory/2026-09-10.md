# 2026-09-10 工作日志

## v0.10.1：ML 打印分页修复——末页正面缺失（用户问询驱动的覆盖完整性审计）

- 用户问"PDF 打印版中多科目明细账排版分页是否有问题" → 建立**程序化审计方法**：解析 xlsx XML 的 _xlnm.Print_Area 原始区域串 + 扫描 sheet 全部非空格 → 校验三项（未覆盖格/区域重叠/区域数奇偶）。**判定标准：内容必须被区域完整覆盖（漏 = 打印丢内容）**。
- 审计结果：21 张 ML/分离页**全部**有问题（101-197 未覆盖格/页，首个即"明 细 帐"标题在末块右半）→ 真缺陷。
- 根因：mlAreaPlan 按"滑动窗口"模型（末块只取左半，注释假设"末块右侧为空"）——**实测末块右半 = 末页正面**（标题/页码"分第1页(右)"/会计科目/表头区全在）→ **ML 物理结构是"块=页"**（右半正面+左半反面同页码，writeMLPageHeader 两侧同页号实锤），旧假设错误。
- 修复：序列改 [占位正, 页k正(右半), 页k反(左半), …]（正先，对齐 GL）；尾部补空白保偶数（跨 sheet 配对，对齐 glAreaPlan）；**补页矩形取 blocks*blockRows 之后的空行区**——首版放在末块装订列与末块正面区域重叠（校验抓到 1 对重叠 → 挪位）。
- 终态：110 张账页三项校验全归零；全量测试 + e2e 脚本绿。教训：**"实测验证过"的注释可能是局部的**（原注释称 Sliding window 已实测，实际只验证了页数配对未验证内容覆盖）。
- 状态：CHANGELOG v0.10.1 + SKILL bump 0.10.1，commit 本地，发版待批准。

## 走偏纠正 + v0.10.1 发版（用户裁决）

- 走偏轨迹（用户指出"傻模型接手几轮完全走偏"）：①"生成个 PDF 我看看"→接手者自造 reportlab 渲染器当 PDF 交付（项目 PDF 只有一条路：打印版 xlsx 在 WPS 导出）②用户反问"CLI 输出的 PDF 是这个？"→答"CLI 无 PDF 能力"+建议装 LibreOffice/加功能（答非所问、偏离形态）③说出建议时还没读过"活页打印 + 需打印标记 + reset 增量打印"链路。
- 三重查证：CLI/技能/git 历史均无"agent 自动导出 PDF"流程；用户记忆的"有 WPS 就能用"= **打印版跨平台 WPS 标定**（print-config 系数/模板库）。
- 本机驱动 WPS 实测：`activate` ✓ 成功；`open POSIX file` ✗ -10004（缺自动化授权）；`System Events` ✗ -1712（缺辅助功能授权）→ 两项授权须用户本人给；`open -a wpsoffice <file>` ✓ 可用（LaunchServices 不受 TCC 限）。用户裁决：**暂不需要"agent 导出 PDF"能力**。
- 跑偏产物处置：`git revert 2da0c0f`（渲染脚本撤出仓库）；MEMORY.md 死引用改为"覆盖校验三项方法（脚本已撤，方法保留）"。
- v0.10.1（ML 末页正面修复）用户批准发版。

### v0.10.1 发版完成（用户批准）
- push main+tag 终态 = 268c570（含 MT revert 渲染脚本 + 修复）；CI Release ✓；v0.10.1 = **Latest**，正文已贴人话节。
- 待用户动作：Windows 端 exe → install-skill → doctor（0.10.1）；已有账套如需新打印版重新 generate。

### v0.10.1 发版完成（用户批准）
- push main+tag 终态 = 268c570（含 MT revert 渲染脚本 + 修复）；CI Release ✓；v0.10.1 = **Latest**，正文已贴人话节。
- 待用户动作：Windows 端 exe → install-skill → doctor（0.10.1）；已有账套如需新打印版重新 generate。
