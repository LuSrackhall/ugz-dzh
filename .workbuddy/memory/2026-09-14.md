# 2026-09-14 工作日志

## v0.10.8：恢复 mac-win-common 的 ML 自定义标定（用户要求）

- 背景：用户查"哪次提交把通用模板尺寸改成纯 win 的" → **41c0ed7**（2026-09-11，v0.10.6）。
- 用户要求：**不要回滚（不用 git revert），而是正向把 ML 段改回他给的标定值**：colScale 1.1198、rowScale 0.996、front/backColScale 1.11809、frontBindingDelta -18、frontDigitDelta.k9 -1、backOuterDelta -9、backSummaryDelta 12。
- 已改：templates/print-config.mac-win-common.json（ml 段恢复 + _comment 标注 2026-09-14 恢复）；gl 段（1.13595 + backOuterDelta -8）与字体三项（digit MV Boli / labelFamily 等线 Light / amountHeaderFamily 等线）保留；SKILL.md 模板表同步；双目录一致；**go build → install-skill**（顺序正确）；全量 7 包绿。
- 状态：CHANGELOG v0.10.8 + SKILL bump 0.10.8，commit 本地，发版待批准。

## v0.10.8 增补：win-standard 的 ML 也按用户标定 + 修复 mac 段误伤

- 用户明确："我只让你配置 win 部分，**包括纯 win 模板中的 ml**" → win-standard 的 windows.ml 也从平台级（全 0）改为用户标定（1.1198/0.996/1.11809/-18/k9-1/-9/+12）✓ 两模板 ML 标定统一。
- **发现并修复 mac 段误伤**：v0.10.6 我用 Python 全局 replace 改 win-standard 的 `"digit": "Noteworthy"` → **把 mac 段的同名键也改了**（→MV Boli）✗ → 本版恢复 Noteworthy ✓。
- **教训（重要）**：**改 JSON 模板的局部字段必须用"结构感知"或"含唯一上下文"的替换**——全局 `str.replace` 会跨平台段误伤（本次即 mac 段被牵连）。已记入记忆。
- 三模板 mac 段终检：win-standard / mac-standard 为 1.0/1.0 + Calibri + Noteworthy；mac-win-common 无 mac 段（用代码默认）——全部与改动前一致 ✓。
- 双目录同步 + go build → install-skill + 全量 7 包绿；CHANGELOG v0.10.8 已含"顺带修复 Mac 端误伤"说明。发版待批准。

### v0.10.8 发版完成（用户批准）
- push main+tag 终态 = 4f61983；CI Release ✓；v0.10.8 = **Latest**，正文已贴人话节。
- 用户下一步：win 端 `config apply win-standard -f`（或 mac-win-common）+ 重新 generate，验 ML 尺寸按标定；mac 端确认无变化。

### v0.10.8 发版完成（用户批准）
- push main+tag 终态 = 4f61983；CI Release ✓；v0.10.8 = **Latest**，正文已贴人话节。
- 用户下一步：win 端 `config apply win-standard -f`（或 mac-win-common）+ 重新 generate，验 ML 尺寸按标定；mac 端确认无变化。
