# 2026-09-15 工作日志

## AGENTS.md 新增「Git 工作区纪律」+ v0.10.9 发版

- 用户要求：agent 自行管理 git 工作区的纪律"仅在技能里说明不够严格"，要写进通用宪法（AGENTS.md）——没有就新建、有就追加。
- 已追加 7 条硬约束（AGENTS.md § Git 工作区纪律）：①提交路径限定（严禁 add -A/./commit -a）②提交前 git status 分清他人改动并原样保留 ③push/merge/tag/发版需用户显式批准（批准只覆盖被问的那步）④派生产物不入 git（xlsx/print/pdf/closing）⑤技能双目录同步顺序（改源→embedded→go build→install-skill，TestSkillMirrorSync 守门）⑥长任务 worktree 隔离+清理 ⑦多平台改动须平台隔离实证（新旧二进制逐单元格对比）。
- 技能 workflows.md 的 git 段改为**指向 AGENTS.md 该节**（单一来源，防分叉）。
- 顺序执行：cp embedded → go build → install-skill → 全量 7 包绿 → 镜像一致。
- v0.10.9（Windows digit 全链路 MV Boli + 宪法补充）用户批准发版。

### v0.10.9 发版完成（用户批准）
- push main+tag 终态 = f6c0cda；CI Release ✓；v0.10.9 = **Latest**，正文已贴人话节。
- 内容：Windows 金额数字全链路 MV Boli + AGENTS.md 新增「Git 工作区纪律」7 条。
- 待用户动作：Windows 端 exe → install-skill → doctor（0.10.9）。

### v0.10.9 发版完成（用户批准）
- push main+tag 终态 = f6c0cda；CI Release ✓；v0.10.9 = **Latest**，正文已贴人话节。
- 内容：Windows 金额数字全链路 MV Boli + AGENTS.md 新增「Git 工作区纪律」7 条。
- 待用户动作：Windows 端 exe → install-skill → doctor（0.10.9）。
