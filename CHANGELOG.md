# 更新日志（CHANGELOG）

本文件回答一个问题：**每个版本对使用者（老板与 agent）意味着什么**。不是提交流水账——流水账看 git log 或 GitHub release 的自动清单；这里写的是人话介绍、行为变更与迁移注意。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；新版本发版时**必须**在顶部补写对应小节（发版纪律见文末）。

---

## v0.9.0 —— 科目体系改造：先定义后生成（2026-09-04）

### 这一版讲了什么

此前，账本科目是"从凭证里长出来的"：`generate` 时凭证里出现什么科目就自动登记什么，属性靠内置的 17 类名称表去猜，猜不到就是"未分类"——而未分类科目不进资产负债表和收支结余表、不参与年末损益结转，错了很难被发现（e2e 真实账本里 `专项应付款-日间照料中心` 就一直被归为未分类）。

这一版把方向倒了过来：**先定义后生成**。科目与期初先经审核表确认（可用 `subjects scan` 从凭证自动生成候选表，或从旧账期末科目余额表转录），`generate` 遇到未定义科目会给出清单并拒绝生成；科目属性对照**财政部财会〔2023〕14号官方 42 个一级科目**内置表，专项应付款、累计折旧、在建工程等此前被"猜错"的科目自动归类正确。此外，把账面此前**静默吞掉错误的两个通道**堵上了：收支结余表对未分类科目不再无痕丢行，年末结转对类别未知且余额≠0 的科目显式告警（此前不结转、不告警、跨年带入新账）。

### 🚀 新增

- **`subjects scan`**：扫凭证目录（递归，宽容解析——不要求凭证号/借贷平衡，能直接吃原始 OCR md），提取出现的科目对与出现频率，生成候选建账审核表 CSV（方向按官方表预填、期初余额留空待旧账转录、新科目排前）。产物可直接回喂 `subjects import`。
- **官方 42 科目常量表内置**（财会〔2023〕14号，101 库存现金 ~ 521 所得税费用）：按名归类自动正确，旧账套零迁移。`401 生产(劳务)成本` 显式归"资产"（期末余额=在产品，进资产负债表存货侧，年末不清零）。
- **`check` 未分类可观测化**：统计行"未分类科目 N 个（余额合计 X 元）"+ 逐科目余额提示——"未分类余额>0 连续 3 个月未处理或跨年带入"即科目体系事故。
- **`gen-close` / `year-close` 告警补全**：类别未知且余额≠0 的科目显式列出（此前静默跳过、跨年带入）。
- **收支结余表**：未分类科目单列展示（不计入合计）——此前直接丢行，损益表无痕漏账。

### ⚠ 行为变更（迁移注意）

- **`generate` 默认拒绝未定义科目**：输出清单（科目 / 出现次数 / 样例凭证 / 样例摘要 / 映射来源）与处理指引——按清单走 `subjects scan`（或旧账转录审核表）→ 确认方向 → `subjects import` → 重跑；OCR 错名先 `map`。**`--allow-new` 为显式逃生**（自动登记，属性可能未分类；临时场景用，日常勿用）。
- openspec 铁律 3 同步修订："科目自动管理（静默入树）"→"先定义后生成"。依赖旧行为的自动化脚本需评估（`rebuild.sh` 按已定义科目重建不受影响；逃生方式见其头部注释）。
- **科目编码体系延后**（非删除）：编码仅作常量表数据字段，存储主键化挂触发器（镇级汇总/带码报送、真实改名事故、手工录入界面立项、跨年比较、配置名单静默失效）——详见 `docs/account-code-design.md` §四。

### 🐛 修复

- **备抵科目属性修正**：`152 累计折旧` / `133 生产性生物资产累计折旧` / `162 累计摊销` 官方为"期末**贷**方余额"，此前误标"借"会误导建账方向（财政部 PDF 原文逐字核对）。
- **`subjects import` 新登记科目同样应用审核表「方向」列**——此前只有已存在科目才设置属性，自创名新科目的确认方向被静默丢弃（需二次 import 才生效）。
- **`validateReviewRow` 不再误拒合并总账父级的子科目**——旧口径把"应付款-养老金"这类合法子科目连同父级一起拒绝。
- **gen-close 预登记"本年收益"**：收紧后首次年结不再被自家闸门拦截（此前系统自己生成的结转凭证会被系统拒绝，且提示是死胡同）。
- **告警/check 余额口径改为"≤快照月最近记录"**：休眠未分类科目（记一笔后无发生）不再漏告警——此前期初回退会把它静默带入新年，而告警看不见它。
- **未定义清单附映射来源**（"映射自 X"）与样例凭证文件名——agent 按清单排障不再扑空。

### 📦 对既有账套的影响

**零迁移**：不需要任何 JSON 变更。已存在的"未分类"科目保持原状，按 `check` 提示用 `subjects import` 补属性或 `map` 归并即可。技能文档同步至 0.9.0（含"先定义后生成"铁律、scan 用法、常见错误表）——升级后请重跑 `ledger install-skill`（版本联动校验见 `ledger doctor`）。

---

## v0.8.4 —— 账本边界铁律（2026-09-04）

多账套同机**串账**防护（9-03 发现的 agent 行为风险）：SKILL.md 顶部新增铁律——一次只处理用户明确指定（或当前工作区）的账套；找不到账本直接问用户，**禁止** `find`/`ls` 遍历全盘"找账本"。CLI 参数越界逻辑本就无，风险在 agent 行为，故修在技能层。

## v0.8.3 —— 用户级技能残留检测移交文档（2026-09-03）

install-skill 的用户级残留检测从 CLI 硬编码改为技能文档指导 agent 自查（CLI 回归纯安装职责）。

## v0.8.2 —— 技能目录纳入 git（2026-09-01）

`.agents/skills/` 正常 add/commit（无需 `-f`），技能版本随仓库演进可追溯。

## v0.8.1 —— install-skill 残留检测（2026-09-01）

安装技能时检测用户级目录（~/.workbuddy 等）的历史技能残留，交互询问移除（`--keep-user-level` 保留），推荐仅仓库级安装。附带确立发版纪律：**push `v*` tag 即 CI 自动 goreleaser 发布**，勿手动 `gh release create`（会撞车 422）。

## v0.8.0 —— 迁移入口三件套（2026-09-01）

面向"已有手工账的村委会迁入"的三个核心能力：

- **建账审核表批量导入**：`subjects import`（登记科目+属性）+ `opening import`（期初余额，**试算平衡强制闸门**，科目存在/属性一致/借贷平衡三重校验 + `--dry-run` 预演）+ `check --vs` 验收比对——一张审核表 CSV 贯穿科目建立与期初导入，替代逐条 `add-manual`。
- **科目顺序接线**：`全局设置.科目顺序` 决定账页 Sheet 排列（GL→合并→ML 分区块），打印版顺序自动跟随。
- **打印版多区域打印区域**：导出 PDF 无空白页且正反配对。

另：技能新增 git 纪律（基线先行/每步一提交/OCR 纠错优先级）。

## v0.7.x 系列（要点节选，2026-08）

- **审计修复（四轮会计/审计专家审查，Change 1~12 全部收敛）**：期初锚定建账月、红字四格式+打印红色字体、合并科目累计并集、合并父级冲突双拦截、跨年结转三告警、投产 5 门槛。
- **报表套件**：资产负债表 / 收支结余表 / 科目汇总表 / 凭证序时簿 / 期初期末表。
- **合并总账**：父级合并账页（月结职责划分、连续月结行/期初丢失/边框三缺陷根治）。
- **安全机制**：`lock` 结账标记、generate 幂等保护、凭证号防线、借贷平衡阻断。
- **打印版位格**：金额拆位、红字红字体、分组边框（单红线在元|角之间、三位分组粗绿线）、正反面独立标定。
- **运维**：`rebuild.sh` 全量重建、`check` xlsx 漂移比对、`doctor` 环境自检。

---

## 发版纪律（给维护者）

1. **先写 CHANGELOG**：在本文件顶部补写新版本小节——一段"这一版讲了什么"（人话，写给老板与 agent）+ 新增/行为变更/修复分组。没有这一节的 tag 不许打。
2. **版本号落盘**：SKILL.md `version:` 更新（`.agents/` 与 `embedded/` 双目录，`embedded/embed_test.go` 守护测试会拦截漂移）；CLI 版本由 goreleaser ldflags 注入。
3. **全量验证**：`go test ./...` + `bash scripts/test-e2e.sh` 全绿。
4. **工作树干净**：提交全部变更（含 `.workbuddy/memory/`——goreleaser 对 dirty 工作树直接 fail）。
5. **发布**：`git tag vX.Y.Z && git push origin main vX.Y.Z` → CI 自动 goreleaser 发布（自动清单按 feat/fix 分组，见 `.goreleaser.yml`）。
6. **发布后**：`gh release edit vX.Y.Z --notes-file <整理好的节>` 把本文件对应小节（人话介绍）贴入 release 正文——自动清单是流水账，人话介绍必须补上。
