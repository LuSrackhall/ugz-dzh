# 更新日志（CHANGELOG）

本文件回答一个问题：**每个版本对使用者（老板与 agent）意味着什么**。不是提交流水账——流水账看 git log 或 GitHub release 的自动清单；这里写的是人话介绍、行为变更与迁移注意。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；新版本发版时**必须**在顶部补写对应小节（发版纪律见文末）。

---

## v0.10.2 —— ML 打印区域顺序纠正：保持"基础列→明细列"，只补缺失页（2026-09-10）

### 这一版讲了什么

v0.10.1 修好了"末页右半缺失"，但**把区域顺序改错了**：ML 的打印模型是"**一页逻辑内容分印两页纸——先左半（基础列+明细 1-4）、后右半（明细 5-14）**"，区域列出顺序即阅读（打印）顺序；v0.10.1 主观照搬总分类账的"正面在前"改成"右半在前"，导致占位页右半与其后右半连续成对、整本双面打印错位（下游实测发现："右侧页连续两个放一起，正反页打印坏事"）。

这一版回到正确模型，做**最小修复**：保持原有"左半 → 右半"顺序不变，**只补上缺失的末页右半**，尾部补一张空白页保持偶数（跨 sheet 双面配对）。

### 🐛 修复

- 区域序列：`[块0右半(占位页)] + 逐块 [左半(基础列), 右半(明细列)] + 空白页`。
- 覆盖校验（未覆盖格 / 区域重叠 / 区域数奇偶 三项）对全部 110 张账页归零。

### 📦 对既有账套的影响

**零迁移**；已生成的打印版需重新 `generate` 更新。技能文档同步至 0.10.2——升级后请重跑 `ledger install-skill`。

---

## v0.10.1 —— 多科目明细账打印分页修复：末页正面不再缺失（2026-09-10）

> ⚠ **该版的区域顺序处理有误**（把"右半在前"当作正反配对、占位页后紧跟右半，双面打印整本错位）——**以 v0.10.2 为准**。

### 这一版讲了什么

下游问询"ML 打印版分页是否有问题" → 覆盖完整性审计证实了一个真实缺陷：**多科目明细账（含分离明细账页）打印版每个 sheet 的"最后一页正面"未纳入打印区域**——导出 PDF 时，每张 ML 账页的最后一页缺正面（标题"明 细 帐"、页码、会计科目、表头区与明细列），打印出来是一张没有头的空纸（或整页缺失）。

根因：打印区域规划的模型理解错误——旧实现按"滑动窗口"取中间块两侧、**末块只取左半**（注释假设"末块右侧结构为空"）。实测末块右半含完整正面页头（标题/页码/科目/表头）——ML 的物理结构是**块=页**（右半正面 + 左半反面，同页码），末块右侧正是末页正面，不是空。

### 🐛 修复

- 区域序列纠正为 `[占位正面, 页1正面, 页1反面, 页2正面, 页2反面, …]`——每页正反配对、正面在前（与总分类账的"正1反1正2反2"顺序对齐）；末页正面不再遗漏。
- 尾部补一张空白页保持偶数（对齐 GL 补页逻辑），保证整本导出跨 sheet 时下一账页从正面开始配对；补页取全部数据块之后的空行区，不与任何内容区域重叠。

### 📦 对既有账套的影响

**零迁移**，但已生成的打印版需要重新 `generate` 才更新（打印版是生成品）。查看版/JSON 余额链/报表完全不受影响。审计方法：对打印版全部 110 张账页做打印区域 vs 内容覆盖校验（未覆盖格、区域重叠、奇偶配对三项全部归零）。技能文档同步至 0.10.1——升级后请重跑 `ledger install-skill`。

---

## v0.10.0 —— 仅账页 PDF 开关 + 废弃配置字段显式告警（2026-09-09）

### 这一版讲了什么

两个轻量但显眼的能力，补齐呈现链的最后一环：

1. **`ledgerOnly` 开关（打印版仅含账页）**：打印配置顶层布尔开关，`true` 时打印版 xlsx 移除全部非账页 Sheet（日记账/各类报表/期初期末表），只保留 总分类账/多科目明细账/分离明细账页——用户在 WPS/Excel 打开打印版**直接"导出 PDF"即纯账页成册**（位格拆分、正反面配对、打印区域全部已精调，无需手动跳页）。默认 `false` = 现状不变（打印版含全部 sheet）。日志账/报表数据不受影响（完整存在于查看版）。
2. **废弃配置字段显式告警**：v0.9.6 的字段更名暴露了一个健壮性缺口——JSON 解析对未知字段静默忽略，改名的旧键会被**无声无视**（用户视角："分离页悄悄消失了"）。现在 generate 加载配置时扫描已知废弃字段名，发现即告警提示迁移（当前清单：`明细账独立科目` → `分离明细账科目`，v0.9.6）。配置失效从"静默消失"变成"一眼看到为什么"。

### ⚠ 行为变更（迁移注意）

- 不配置 `ledgerOnly` → 一切照旧；配 `true` 后打印版文件不再含日记账/报表（它们完整存在于查看版，随时可看）。
- JSON 里若还写着 v0.9.5 的旧字段名 `明细账独立科目`，generate 会打 ⚠ 告警提示改为 `分离明细账科目`——照做即恢复分离页。

### 📦 对既有账套的影响

**零迁移**。技能文档同步至 0.10.0——升级后请重跑 `ledger install-skill`。

---

## v0.9.9 —— 明细列净额按科目方向定向：红字语义修正（2026-09-09）

### 这一版讲了什么

下游审计发现：多科目明细账"金额分析"列的净额此前**统一按 借−贷 计算，与科目方向无关**；而打印版规则是"负数=红字"（手工账红笔惯例）。两者组合对借方科目（费用/资产类）碰巧正确，对**贷方科目（收入/负债/权益类）整列语义颠倒**——正常收入发生额显示为红字、冲减和红字冲销反而显示蓝字。

这一版把明细列净额**按科目基准方向定向**：贷方向科目 = 贷−借（正常发生为正/蓝字），其余 = 借−贷；负值 = 与基准方向相反的发生额（冲减/红字冲销）→ 打印红字。"红字"从此严格表示"反向发生额"，与手工账多栏账红笔惯例完全一致。

### ⚠ 行为变更（迁移注意）

- **贷方向科目（收入/负债/权益类）的明细列符号翻转**：正常发生由负变正、冲减由正变负。数字的绝对值不变，变的只是符号方向（打印版红字/蓝字随之纠正）。
- 已生成的历史月份账页**不回改**（铁律一）；对历史月份 `generate -f` 重建即可按新口径呈现。
- 借方向科目（费用/资产/成本类）行为不变。方向判定：科目树叶子属性（subjects import 确认值）优先，官方科目表/大类推断兜底；总账"借/贷"方向列与余额列不受影响。

### 📦 对既有账套的影响

**零迁移**（有收入/负债/权益类多科目明细账且在意呈现口径的账套，可 `-f` 重建历史月）。技能文档同步至 0.9.9——升级后请重跑 `ledger install-skill`。

---

## v0.9.8 —— ML 类账页期初行标签对齐手工账规范（2026-09-08）

### 这一版讲了什么

下游评价者反馈 + 实测确认：ML 类账页（合并多科目明细账/分离明细账页）的期初行标签与 GL 不一致、不合手工账规范。两处修正：

1. **1 月跨年延续（期初≠0）**：摘要由"期初余额"改为 **"上年结转"**——对齐 GL 与手工账规范（跨年首行登记上年余额）。
2. **期初=0 不再写"承前页 平 0"占位行**：首数据页无上页可承，"承前页"用词错位（gen-close 清零损益后，新年首月损益科目的"承前页 平"即此）。修正后首笔分录直接顶格，余额链从 0 起算无断裂——与 GL 行为对齐（GL 从不为 0 期初写行）。

"承前页"标签此后只用于**页间衔接**（翻页后的新页首行），跨年/建账/占位不再借用。

### ⚠ 行为变更（迁移注意）

- 新年首月的 ML 类账页首行形态变化：损益类（gen-close 清零后）不再有"承前页 平"占位行；跨年延续行显示"上年结转"。
- 已生成的历史月份账页**不回改**（铁律一）；如需新年账本呈现新规范，对新年首月 `generate -f` 重建即可。JSON 余额链/报表完全不受影响（拆的是呈现不是账）。

### 📦 对既有账套的影响

**零迁移**。技能文档同步至 0.9.8——升级后请重跑 `ledger install-skill`。

---

## v0.9.7 —— 分离明细账页页序联动明细列顺序（2026-09-08）

### 这一版讲了什么

多科目明细账的"明细列顺序"此前只管合并页里列从左到右的次序；分离明细账科目引入后，同一次序概念多了一种呈现——每明细一页。这一版把两者接通：**分离明细账页的组内页序自动跟随 明细列顺序**（列序=页序，一个心智）。同一本账里，合并页从左到右的明细列与后面的分离页一一对应，翻阅连贯，零新增配置。

### 🚀 新增

- **分离页组内次序联动**：`明细列顺序` 里配了某总账的列序（如 `公益支出: [补助费用, 公益事业支出, …]`）时，`明细账-公益支出-*` 分离页按此次序排列；未列出的明细按 Sheet 名兜底殿后。
- **优先级定级**：`科目顺序`（显式页序）> `明细列顺序`（分离页组内次序）> 按名兜底——`科目顺序` 显式条目优先，列序不越权。

### 📦 对既有账套的影响

**零迁移**。未配 `明细列顺序` 的总账，其分离页维持按名排序（行为不变）；配了列序的自动跟随。技能文档同步至 0.9.7——升级后请重跑 `ledger install-skill`。

---

## v0.9.6 —— 分离明细账科目：按总账配置 + 互不冲突共存（2026-09-08）

### 这一版讲了什么

v0.9.5 引入的明细科目独立账页，按用户模型重新校准了两点：

1. **配置按总账科目名**（对仗"合并总账科目"之于 GL）：配 `["公益支出"]` → 公益支出**下面所有明细科目**各自拿到一张独立账页 `明细账-公益支出-<明细>`（标准 e2e 实测 9 个明细全部分离），不必逐个明细去配。也支持叶子全路径写法做单明细分离（双匹配，对仗"总分类账忽略科目"）。
2. **分离页与合并页互不冲突共存**（对仗 GL 叶子页与合并页共存）：合并多科目明细账的明细列**照常保留**（v0.9.5 的"列收缩"撤除，不做删减），分离页是额外视图。

### ⚠ 行为变更（迁移注意）

- **配置字段更名**：`明细账独立科目`（v0.9.5）→ **`分离明细账科目`**（v0.9.6）。v0.9.5 的字段名作废——若已按 v0.9.5 配置过，请把键名改为"分离明细账科目"（配置值写法不变，且现在推荐直接写总账科目名）。
- 合并 ML 页不再因分离配置收缩明细列（v0.9.5 的"残留列报错提示 -f"随之取消）——合并页与分离页共存，无需 `-f` 重算合并页。
- 其余语义不变：GL 叶子页照常、JSON/报表零改动、合并父级禁止同时配置分离、配置移除后孤儿页自动删除。

### 📦 对既有账套的影响

零迁移（v0.9.5 发布当日更名，无存量配置）。技能文档同步至 0.9.6——升级后请重跑 `ledger install-skill`。

---

## v0.9.5 —— 明细科目独立账页：明细账独立科目（2026-09-08）

> ⚠ **当日即被 v0.9.6 取代**：配置字段更名为 **分离明细账科目**（原"明细账独立科目"作废），语义升级为按总账科目名配置 + 与合并页互不冲突共存，见 v0.9.6 节。

### 这一版讲了什么

多科目明细账此前只有"合并"一种形态：一个总账科目一页，明细科目挤在"金额分析"列里。少数场景需要对**某个明细科目单独核算**（≈三级科目私下细分）——这一版支持把它从合并页**分离**出来，单独开一张账页 `明细账-管理费用-办公费`。账页样式仍是多科目明细账那张多栏式纸（四行表头、左右双面、明细列区域），只是单明细科目立页无下级明细，**明细列自然为空**。对仗已有的两个配置：总分类账默认分类、合并需配置（合并总账科目）；多科目明细账默认合并、**分离现在也可配置**（明细账独立科目）。

### 🚀 新增

- **配置 `全局设置.明细账独立科目`**：叶子科目全路径数组（如 `["管理费用-办公费"]`），精确匹配。**默认空 = 完全不生效**（零回归——不配置的账本行为逐字节不变）。
- **独立账页全套**：期初行（1 月跨年延续"期初余额"、调整额生效同规则）、分录逐笔、过次页/承前页翻页、月结四行（本月合计/本季合计/本年累计/期末余额）、跨月余额链续写——与合并多科目明细账同族同源（共享写入本体）。
- **合并页列收缩**：命中名单的明细自动从合并多科目明细账的明细列排除（非留空列）；余额列口径不变（仍含该明细发生额，拆的是页不是账）。
- **打印版同步**：独立账页走 ML 拆位变换（借/贷/余 11 位、明细 10 位），`print/` 目录打印版与合并页同构。
- **排序区块**：账页顺序 总分类账 → 合并总账 → 多科目明细账 → **明细账独立页**（殿后）；`科目顺序` 配置对独立页照常生效。

### ⚠ 行为变更（迁移注意）

- 不配置 = 零变化。配置后 GL 叶子页照常生成（互不影响，加法路由）——独立账页是**额外**视图，不是搬移。
- **名单变更需 `-f` 从首月重建**：新增名单时存量合并页仍含其列 → generate 直接报错提示（不会静默错账）；移出名单后孤儿独立页下次 generate 自动删除。
- 名单项必须是叶子全路径（科目路径含连字符，不做前缀匹配）；不得是合并总账父级（报错拦截）。

### 📦 对既有账套的影响

**零迁移**。技能文档同步至 0.9.5——升级后请重跑 `ledger install-skill`。

---

## v0.9.4 —— 年末二段结转：本年收益 → 收益分配（2026-09-07）

### 这一版讲了什么

对照财会〔2023〕14号补上年末结转的最后一处制度缺口。此前 `gen-close` 只做第一段（收入/费用 → 本年收益），上年净收益以"本年收益"期初滞留带入新年——次年再结转时两年净额混在同一科目，"本年收益"失去年度语义（演示账套实测：2026-01 期初挂着 2025 年净收益 263 万）。这一版 gen-close **默认两段齐发**：第一段损益结转后追加二段凭证 本年收益 → 收益分配——未分配收益（净收益：借 本年收益 / 贷 收益分配；净亏损方向相反），年末"本年收益"归零、净额挂账，新年从 0 起算。

### 🚀 新增

- **gen-close 二段凭证**（默认生成；`--no-transfer` 跳过）：金额按第一段并入后"本年收益"的重算余额（期初 + 全部损益科目发生——干净首跑、一段已生成未合并、一段已合并的老账套三种状态求和同值，重跑一次全部自愈）。目标科目挂官方 322 明细 **收益分配-未分配收益**，净亏损为借余（未弥补亏损）。
- **二段独立防重**：closing/ 已有含"收益分配"分录的凭证即跳过（一段凭证本身含"本年收益"，不能沿用旧口径）；closing/ 按年隔离，跨年不串。
- **结转目标科目自动登记**：本年收益、收益分配-未分配收益（属性=贷），与一段同模式——先定义后生成不被自家凭证拦截。
- **资产负债表权益列口径固化**（报表代码零改动，e2e 断言）：结转月权益列显示 收益分配-未分配收益（即官方"未分配收益"项目，净亏损红字显负）；未结转月照旧"本年收益（未结转损益）"行；年内既有结转历史又有未结转损益时两行并存。

### ⚠ 行为变更（迁移注意）

- gen-close 现在每次会多出一张"年末收益结转"凭证（编号顺延）；两段并入后 损益/本年收益 年末=0、净额在 收益分配-未分配收益。只想要一段老行为的加 `--no-transfer`（下次不带 flag 重跑会自动补发二段）。
- 已用旧版做过一段结转的账套**无需回溯重建**（历史月铁律一不动）：升级后第一次跑 gen-close 即按重算余额补发二段，重新 generate 余额月并入即可；year-close 不变、仍不清零任何科目。

### 📦 对既有账套的影响

**零迁移**。技能文档同步至 0.9.4（gen-close 两段行为、权益列口径、年末流程顺序）——升级后请重跑 `ledger install-skill`。

---

## v0.9.3 —— 总分类账忽略科目 全面生效（2026-09-06）

### 这一版讲了什么

下游实测：配置 `总分类账忽略科目`（父级名或叶子全路径）后，"总分类账-固定资产-×××" 叶子 sheet 仍全部生成（67 个）——分录虽被抑制，但期初行路径照建 sheet、月结照写、存量 sheet 随复制链永久存活，`-f` 重建也带回来。这一版把抑制挂到全部四条路径，并让存量 sheet 在下次 generate 时自动清除。顺带修复一个同源存量缺陷：年中新建（或忽略后重新包含）的科目账页**不显示期初余额行**——余额数字一直正确，但账页上看不到期初。

### 🐛 修复

- **忽略名单按分录全路径匹配**：父级名忽略整棵子树、叶子全路径精确忽略，二者均生效（此前名单只与凭证总帐科目段比对，叶子全路径名单永不命中）。
- **期初行/月结补挂抑制**：被忽略科目不再被建 sheet（期初行路径）、不再写月结（此前分录被跳过但月结照写，账页内容自相矛盾；sheet 缺失时还会硬报错）。
- **存量清理**：每次 generate 按当前配置删除被忽略叶子的 sheet（合并父级汇总视图豁免）——生成下月或 `-f` 当月即收敛。
- **账页期初行死分支**：`ensureGLSheet` 返回新建标志（对齐合并页 D2 修法）——年中新建/重新包含的科目账页现在显示期初余额行（此前 `isNew` 在标题块写入后恒为 false，成为死分支）。

### ⚠ 行为变更（迁移注意）

- 配置/取消忽略科目后**无需从建账月重建**：生成下月或 `-f` 当月即收敛。取消忽略后科目账页从当月重建，余额链从期初行正确起算，此前月份的账页明细不在新 sheet 上（历史以 JSON 为准）。
- 被忽略科目的期初此后仅由 JSON 权威链提供（无账页链兜底）；`check` 的 xlsx 漂移比对对被忽略科目不生效——均已写入 `json-schema.md`。

### 📦 对既有账套的影响

**零迁移**。未配置忽略的账套行为零变化（真实账套回归：sheet 集合、期初行、余额链逐项验证不变）；已配置的账套升级后生成下月即见效。技能文档同步至 0.9.3——升级后请重跑 `ledger install-skill`。

---

## v0.9.2 —— 科目余额表：期初+本月发生+期末 逐科目一体（2026-09-06）

### 这一版讲了什么

对账与交付场景缺一张"标准科目余额表"：此前期初、借贷发生、期末分散在期初表/科目汇总表/期末表三张 sheet，拿旧财务软件导出的《科目余额表》来对账要来回翻三张表。这一版把四列拼成账本内一张新 sheet（科目 | 方向 | 期初余额 | 借方发生额 | 贷方发生额 | 期末余额），行同时覆盖明细与一级汇总（科目树只定义叶子时自动补一级科目行，与旧软件"上级行=下级合计"一致），合计行与凭证序时簿三方勾稽平衡。发布动机：下游正拿本系统与旧账逐月对账。

### 🚀 新增

- **账本内「科目余额表」sheet**（每期 generate 生成）：明细行 + 父级/一级汇总行（子树求和）；全 0 科目不显示；期初/期末按科目属性归一显示（借方科目借余为正、贷方科目贷余为正，**反向余额显负**——应收款净贷余、应付款净借余这类异常一眼可见）；`--allow-new` 当月新科目方向按官方科目表兜底。合计行借贷发生额按实际记账科目求和（父子不双算），借≠贷时追加差额提示行。
- 数据可信度经三重复核：红队对抗审计（无 blocker/major）+ 独立脚本全量重算（94 行行集/四列数值/方向归一逐行吻合）+ 三方勾稽（科目余额表 ↔ 科目汇总表 ↔ 凭证序时簿合计相等且借贷平衡）。

### ⚠ 行为变更（迁移注意）

- **balance.xlsx 内 sheet 更名「科目发生额汇总」**（原「科目余额表」）：该文件是当月发生额净额口径，与账本内新表同名不同物，故让出标准名。文件名 balance.xlsx / balance.csv 不变，无程序化读取方受影响。

### 📦 对既有账套的影响

**零迁移**：不需要任何 JSON 变更；已有月份 xlsx 依铁律一不动，新表随下次 generate（新月或 -f 重建）出现。技能文档同步至 0.9.2——升级后请重跑 `ledger install-skill`。

---

## v0.9.1 —— 期初回退自愈：账页链与 JSON 冲突时采信 JSON（2026-09-06）

### 这一版讲了什么

下游实测发现一个 0.9.0 bug：科目被凭证**冲平**（余额归 0）后静置一两个月再重现时，期初被错误重置回建账期初（宏远公司1 六月冲平、八月重现期初又变回 1,003 万）；更麻烦的是，手工把 JSON 余额链修对之后，用 `-f` 重跑历史月份还会复发。

根因是期初计算有两个来源——上月账本 xlsx 的账页余额链，和 JSON 科目树的余额历史——两者不一致时旧版无条件相信 xlsx 链。健康账本里两源恒等，问题只出现在"xlsx 链过期"（冲平/更正凭证没有级联重建进 xlsx）而 JSON 已经修对的状态：`-f` 从陈旧账页读回旧余额，把错误重新写回 JSON。这一版把优先级改成符合铁律三（JSON 是余额唯一权威源）：**双源一致时行为完全不变；不一致时采信 JSON 并打印告警**（列出双方数值与重建指引）——`-f` 重建从"复发"变成"自愈"。

### 🐛 修复

- **期初双源冲突采信 JSON**：`generate` 期初计算在 xlsx 账页链与 JSON 余额链不一致时，按铁律三采信 JSON 并告警（`⚠ 科目 X 期初双源冲突：账本页链期末 A 元，JSON 权威链 B 月期末 C 元——已按铁律三采信 JSON……`）——冲平科目静置重现不再复活旧余额，手修 JSON 后 `-f` 重建自愈而非复发。仅单源存在（如跨年首月不复制上年工作簿、忽略科目无账页）时行为不变。
- 技能"常见错误与处理"表补充双源冲突告警的解法行（版本随版同步 0.9.1）。

### 📦 对既有账套的影响

**零迁移**：健康账本两源恒等，所有生成结果与 0.9.0 完全一致（回归测试固化）。若升级后看到"双源冲突"告警，说明账本 xlsx 与 JSON 确有分歧（典型：某月凭证改动后未 `-f` 级联重建，或手工改过一侧）——按告警提示核对后从冲突月 `-f` 重建即可，重建会以 JSON 为准自愈。

---

## v0.9.0 —— 科目体系改造：先定义后生成（2026-09-04）

### 这一版讲了什么

此前，账本科目是"从凭证里长出来的"：`generate` 时凭证里出现什么科目就自动登记什么，属性靠内置的 17 类名称表去猜，猜不到就是"未分类"——而未分类科目不进资产负债表和收支结余表、不参与年末损益结转，错了很难被发现（e2e 真实账本里 `专项应付款-日间照料中心` 就一直被归为未分类）。

这一版把方向倒了过来：**先定义后生成**。科目与期初先经审核表确认（可用 `subjects scan` 从凭证自动生成候选表，或从旧账期末科目余额表转录），`generate` 遇到未定义科目会给出清单并拒绝生成；科目属性对照**财政部财会〔2023〕14号官方 42 个一级科目**内置表，专项应付款、累计折旧、在建工程等此前被"猜错"的科目自动归类正确。此外，把账面此前**静默吞掉错误的两个通道**堵上了：收支结余表对未分类科目不再无痕丢行，年末结转对类别未知且余额≠0 的科目显式告警（此前不结转、不告警、跨年带入新账）。

### 🚀 新增

- **`subjects scan`**：扫凭证目录（递归，宽容解析——不要求凭证号/借贷平衡，能直接吃原始 OCR md），提取出现的科目对与出现频率，生成候选建账审核表 CSV（方向按官方表预填、期初余额留空待旧账转录、新科目排前）。产物可直接回喂 `subjects import`。
- **官方 42 科目常量表内置**（财会〔2023〕14号，101 库存现金 ~ 521 所得税费用）：按名归类自动正确，旧账套零迁移。`401 生产(劳务)成本` 显式归"资产"（期末余额=在产品，进资产负债表存货侧，年末不清零）。
- **`check` 未分类可观测化**：统计行"未分类科目 N 个（余额合计 X 元）"+ 逐科目余额提示——"未分类余额>0 连续 3 个月未处理或跨年带入"即科目体系事故。
- **`gen-close` / `year-close` 告警补全**：类别未知且余额≠0 的科目显式列出（此前静默跳过、跨年带入）。
- **收支结余表**：未分类科目单列展示（不计入合计）——此前直接丢行，损益表无痕漏账。

### ⚠ 行为变更（迁移注意）

- **`generate` 默认拒绝未定义科目**：输出清单（科目 / 出现次数 / 样例凭证 / 样例摘要 / 映射来源）与处理指引——按清单走 `subjects scan`（或旧账转录审核表）→ 确认方向 → `subjects import` → 重跑；OCR 错名先 `map`。**`--allow-new` 为显式逃生**（自动登记，属性可能未分类；临时场景用，日常勿用）。
- openspec 铁律 3 同步修订："科目自动管理（静默入树）"→"先定义后生成"。依赖旧行为的自动化脚本需评估（`rebuild.sh` 按已定义科目重建不受影响；逃生方式见其头部注释）。
- **科目编码体系延后**（非删除）：编码仅作常量表数据字段，存储主键化挂触发器（镇级汇总/带码报送、真实改名事故、手工录入界面立项、跨年比较、配置名单静默失效）——详见 `docs/account-code-design.md` §四。

### 🐛 修复

- **备抵科目属性修正**：`152 累计折旧` / `133 生产性生物资产累计折旧` / `162 累计摊销` 官方为"期末**贷**方余额"，此前误标"借"会误导建账方向（财政部 PDF 原文逐字核对）。
- **`subjects import` 新登记科目同样应用审核表「方向」列**——此前只有已存在科目才设置属性，自创名新科目的确认方向被静默丢弃（需二次 import 才生效）。
- **`validateReviewRow` 不再误拒合并总账父级的子科目**——旧口径把"应付款-养老金"这类合法子科目连同父级一起拒绝。
- **gen-close 预登记"本年收益"**：收紧后首次年结不再被自家闸门拦截（此前系统自己生成的结转凭证会被系统拒绝，且提示是死胡同）。
- **告警/check 余额口径改为"≤快照月最近记录"**：休眠未分类科目（记一笔后无发生）不再漏告警——此前期初回退会把它静默带入新年，而告警看不见它。
- **未定义清单附映射来源**（"映射自 X"）与样例凭证文件名——agent 按清单排障不再扑空。

### 📦 对既有账套的影响

**零迁移**：不需要任何 JSON 变更。已存在的"未分类"科目保持原状，按 `check` 提示用 `subjects import` 补属性或 `map` 归并即可。技能文档同步至 0.9.0（含"先定义后生成"铁律、scan 用法、常见错误表）——升级后请重跑 `ledger install-skill`（版本联动校验见 `ledger doctor`）。

---

## v0.8.4 —— 账本边界铁律（2026-09-04）

多账套同机**串账**防护（9-03 发现的 agent 行为风险）：SKILL.md 顶部新增铁律——一次只处理用户明确指定（或当前工作区）的账套；找不到账本直接问用户，**禁止** `find`/`ls` 遍历全盘"找账本"。CLI 参数越界逻辑本就无，风险在 agent 行为，故修在技能层。

## v0.8.3 —— 用户级技能残留检测移交文档（2026-09-03）

install-skill 的用户级残留检测从 CLI 硬编码改为技能文档指导 agent 自查（CLI 回归纯安装职责）。

## v0.8.2 —— 技能目录纳入 git（2026-09-01）

`.agents/skills/` 正常 add/commit（无需 `-f`），技能版本随仓库演进可追溯。

## v0.8.1 —— install-skill 残留检测（2026-09-01）

安装技能时检测用户级目录（~/.workbuddy 等）的历史技能残留，交互询问移除（`--keep-user-level` 保留），推荐仅仓库级安装。附带确立发版纪律：**push `v*` tag 即 CI 自动 goreleaser 发布**，勿手动 `gh release create`（会撞车 422）。

## v0.8.0 —— 迁移入口三件套（2026-09-01）

面向"已有手工账的村委会迁入"的三个核心能力：

- **建账审核表批量导入**：`subjects import`（登记科目+属性）+ `opening import`（期初余额，**试算平衡强制闸门**，科目存在/属性一致/借贷平衡三重校验 + `--dry-run` 预演）+ `check --vs` 验收比对——一张审核表 CSV 贯穿科目建立与期初导入，替代逐条 `add-manual`。
- **科目顺序接线**：`全局设置.科目顺序` 决定账页 Sheet 排列（GL→合并→ML 分区块），打印版顺序自动跟随。
- **打印版多区域打印区域**：导出 PDF 无空白页且正反配对。

另：技能新增 git 纪律（基线先行/每步一提交/OCR 纠错优先级）。

## v0.7.x 系列（要点节选，2026-08）

- **审计修复（四轮会计/审计专家审查，Change 1~12 全部收敛）**：期初锚定建账月、红字四格式+打印红色字体、合并科目累计并集、合并父级冲突双拦截、跨年结转三告警、投产 5 门槛。
- **报表套件**：资产负债表 / 收支结余表 / 科目汇总表 / 凭证序时簿 / 期初期末表。
- **合并总账**：父级合并账页（月结职责划分、连续月结行/期初丢失/边框三缺陷根治）。
- **安全机制**：`lock` 结账标记、generate 幂等保护、凭证号防线、借贷平衡阻断。
- **打印版位格**：金额拆位、红字红字体、分组边框（单红线在元|角之间、三位分组粗绿线）、正反面独立标定。
- **运维**：`rebuild.sh` 全量重建、`check` xlsx 漂移比对、`doctor` 环境自检。

---

## 发版纪律（给维护者）

1. **先写 CHANGELOG**：在本文件顶部补写新版本小节——一段"这一版讲了什么"（人话，写给老板与 agent）+ 新增/行为变更/修复分组。没有这一节的 tag 不许打。
2. **版本号落盘**：SKILL.md `version:` 更新（`.agents/` 与 `embedded/` 双目录，`embedded/embed_test.go` 守护测试会拦截漂移）；CLI 版本由 goreleaser ldflags 注入。
3. **全量验证**：`go test ./...` + `bash scripts/test-e2e.sh` 全绿。
4. **工作树干净**：提交全部变更（含 `.workbuddy/memory/`——goreleaser 对 dirty 工作树直接 fail）。
5. **发布**：`git tag vX.Y.Z && git push origin main vX.Y.Z` → CI 自动 goreleaser 发布（自动清单按 feat/fix 分组，见 `.goreleaser.yml`）。
6. **发布后**：`gh release edit vX.Y.Z --notes-file <整理好的节>` 把本文件对应小节（人话介绍）贴入 release 正文——自动清单是流水账，人话介绍必须补上。
