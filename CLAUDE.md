# 手工账电子化生成系统

## 开发规范

实现任何核心模块（balance、generator、main.go）前，先阅读 `openspec/project.md` 确认方案与总体规划一致。如有不匹配，暂停并提问，不要静默偏离。

## 提交规范

所有 git commit message 使用中文。格式：

```
feat(simulation): 新增朝向系统组件
fix(render): 修复盾牌血条显示问题
docs: 更新设计文档
```

## 历史变更记录

项目历史变更记录存放在 `openspec/changes/archive/` 和 `openspec/specs/` 中。

## 页面布局宪法

所有 Excel 输出中涉及行列坐标的渲染代码**必须**遵守以下架构。

### 核心思想

物理纸张的尺寸驱动所有坐标计算，不依赖 Excel 页眉/页脚/边距功能。页面边距、装订线、页间隙均由空白行列模拟。

### 架构

```
LayoutSpec（物理约束）
       │
       ▼
ComputeLayout（纯函数）
       │
       ▼
Layout（计算坐标：每列的起始列/宽度，每行的起始行/高度）
       │
       ▼
Renderer（Excel/未来其他写入器——只消费 Layout，不知晓物理单位）
```

### 硬规则

1. **物理驱动**：所有坐标以 mm 为单位定义。`ComputeLayout(spec)` 接收物理约束，输出设计行列号。
2. **两层结构**：
   - 外层：物理限制（纸张尺寸、装订边、页间隙、每页行数）——永不变
   - 内层：区域内列/行拆分——可按需变更，**不影响其他区域**
3. **区域独立**：任意区域宽度/高度变化，不破坏其他区域的物理边界。区域内拆分前后总面积不变。
4. **每个账页类型一个 Spec**（总分类账、多科目明细账等各自定义 Spec）。
5. **禁止硬编码**：渲染代码中不允许出现硬写的列号/行号；所有坐标必须通过 `ComputeLayout(spec)` 计算。
6. **正/反面同 Sheet**：每页正面和反面内容并排在同一个 Sheet 中。
