# 手工账电子化生成系统

## 开发规范

实现任何核心模块（balance、generator、main.go）前，先阅读 `openspec/project.md` 确认方案与总体规划一致。如有不匹配，暂停并提问，不要静默偏离。

## 提交规范

所有 git commit message 使用中文。格式：

```
feat(simulation): 新增朝向系统组件
fix(render): 修复盾牌血条显示问题
docs: 更新设计文档
```

## 历史变更记录

项目历史变更记录存放在 `openspec/changes/archive/` 和 `openspec/specs/` 中。

## 页面布局宪法

所有 Excel 打印输出（总分类账、多科目明细账、未来其他账页）**必须**遵守以下架构：

### 核心思想
物理纸张（A4）的尺寸驱动所有行列坐标计算，不依赖 Excel 页眉/页脚/边距功能。页面边距、装订线、页间隙均由空白行列模拟。

### 架构分层

```
LayoutEngine（宪法）
  ┌─────────────────────────────────────┐
  │ Spec（物理约束）→ 坐标计算器（纯函数）  │
  │ 输入：纸张尺寸/装订边/每页行数/列比例  │
  │ 输出：每列起始列/每行起始行/宽度/高度  │
  └─────────┬───────────────────────────┘
            │
    ┌───────┼───────────────┐
    │       │               │
  总分类账  多科目明细账   未来其他账页
   Spec     Spec          Spec
```

### 宪法规则

1. **物理驱动**：所有坐标以 **mm** 为单位定义（A4 横向 297×210mm）。`ComputeLayout(spec)` 接收物理约束，输出每个设计列在设计行中的起始 mm 位置和宽度/高度。渲染器（Excel）将 mm 坐标按比例映射为 Excel 列宽和行高。**渲染器不知晓物理单位，只接收设计列号/行号。**
2. **两层结构**：
   - 外层：物理限制（纸张尺寸、装订边 20mm、页间隙 5mm、每页行数）—— 永不变
   - 内层：区域内列/行分配（如日期区、金额分栏区）—— 可按需拆分，**不影响其他区域**
3. **区域独立**：任意区域宽度/高度变化，不破坏其他区域的物理边界。例如：
   - 日期列拆为"月""日"两列 → 日期区总宽不变
   - 金额位数从 12 变 14 → 金额区总宽不变，每位宽度等分
4. **LayoutSpec 结构**：每个账页类型（总分类账、多科目明细账）有独立的 Spec，定义其列分配比例和行布局
5. **禁止硬编码**：不允许在渲染代码中硬写列号/行号；所有行列必须通过 `ComputeLayout(spec)` 计算得到
6. **正/反面同 Sheet**：每一页的正面和反面内容并排在同一个 Sheet 中，打印时选区打印

### 通用页内布局（所有账页类型共用）

```
标题区（高度 = 约 5 行）：
  行: 分第 n 页（绿色，n 红色，虚线底线）
  行: 总   分   类   账（深绿加粗，双下划线，字间距 ~1.5 字）
  行: 科目名称（印章红）
  [空行]
列标题行（1 行）：
  行: 日期│凭证号│摘要│借方金额│贷方金额│方向│余额│金额分栏...
数据区（N 行，每页固定）：
  行: 数据行 1~N（N = Spec 定义的每页行数）
    其中过次页/承前页与数据行高度一致
```

### 实现步骤

1. 先实现 `LayoutEngine` 包（`generator/layout/`），含 `LayoutSpec`、`ComputeLayout()`
2. 总分类账、多科目明细账各自定义 Spec，调用 `ComputeLayout` 获取坐标
3. 修改 `appendToGLSheet` 等写入逻辑，用计算坐标代替硬编码行列
4. 后续任何列/行调整，只改 Spec，不改写入逻辑
