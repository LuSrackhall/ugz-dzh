# 手工账电子化生成系统

## 开发规范

实现任何核心模块（balance、generator、main.go）前，先阅读 `openspec/project.md` 确认方案与总体规划一致。如有不匹配，暂停并提问，不要静默偏离。

## 提交规范

所有 git commit message 使用中文。格式：

```
feat(simulation): 新增朝向系统组件
fix(render): 修复盾牌血条显示问题
docs: 更新设计文档
```

## 历史变更记录

项目历史变更记录存放在 `openspec/changes/archive/` 和 `openspec/specs/` 中。

# 页面布局宪法（Layout Constitution）

> **本宪法是本项目所有账页模板的最高设计原则。**
>
> 本宪法约束的是**布局设计与演进方式**，而不是某一种账页或某一种渲染实现。
>
> 所有 AI Agent、开发者及未来维护者，在涉及任何页面布局修改时，都必须遵守本宪法。

---

# 一、最高原则

## 1.1 区域优先，而不是单元格优先

页面布局必须以**业务区域（Region）**作为设计单位，而不是 Excel 单元格。

AI Agent 在收到任何布局需求时，应首先思考：

> **这是哪个业务区域的变化？**

而不是：

> **应该修改哪个单元格？**

页面中的任何修改，都必须优先发生在所属区域内部。

---

## 1.2 布局服务于演进，而不是动态运行

布局计算存在的唯一目的，是降低模板设计阶段的迭代成本。

它不是运行时布局引擎，也不是响应式布局系统。

项目最终交付的是：

> **固定、稳定、可打印的页面模板。**

因此：

- 模板最终固定
- 坐标最终固定
- Renderer 最终固定

但这些固定结果，都必须来源于统一的布局计算，而不是人工维护。

---

## 1.3 坐标是结果，而不是设计输入

任何：

- 行号
- 列号
- 合并区域
- 行高
- 列宽
- Excel 坐标

都属于布局计算结果。

不得作为设计依据。

---

# 二、布局模型

所有页面统一采用以下模型：

```
LayoutSpec
      │
      ▼
ComputeLayout(spec)
      │
      ▼
Layout
      │
      ▼
Renderer
```

---

## 2.1 LayoutSpec

描述页面结构。

例如：

- 页面尺寸
- 页面方向
- 装订边
- 页间距
- 每页业务行数
- 各业务区域
- 区域内部结构
- 默认比例

LayoutSpec 不包含任何 Renderer 逻辑。

---

## 2.2 ComputeLayout

ComputeLayout 是唯一允许计算布局的位置。

负责：

- 行号
- 列号
- 起始位置
- 行高
- 列宽
- 合并范围
- 各区域最终坐标

ComputeLayout 必须保持纯函数。

即：

相同输入，

得到相同输出。

不得依赖运行状态。

---

## 2.3 Layout

Layout 是页面布局快照。

包含：

- 所有区域位置
- 所有最终坐标
- 所有宽高
- 所有跨度

Renderer 只能消费 Layout。

不得修改 Layout。

---

## 2.4 Renderer

Renderer 负责：

将 Layout 输出为最终页面。

Renderer 不参与：

- 布局设计
- 布局计算
- 布局修正

---

# 三、区域设计原则

## 3.1 页面先划分区域

页面必须首先划分业务区域。

例如：

```
Page
├── Header
├── TableHeader
├── Body
└── Footer
```

不同账页可以拥有不同区域。

但必须先完成区域划分。

再进行区域内部设计。

---

## 3.2 区域拥有稳定边界

每个区域拥有自己的：

- 起始位置
- 宽度
- 高度

区域边界代表业务结构。

除修改 LayoutSpec 外，

不得随意改变。

---

## 3.3 区域之间保持独立

区域之间互不依赖。

例如：

Header 不应该知道：

Body 有多少列。

Body 也不应该知道：

Header 有多少行。

区域只能通过 Layout 协作。

不得直接耦合。

---

## 3.4 区域内部允许自由演进

区域内部允许：

- 拆列
- 合列
- 拆行
- 合行
- 增加辅助列
- 删除辅助列
- 调整比例
- 调整展示方式

这是正常演进。

---

## 3.5 区域整体尺寸保持稳定

区域内部如何变化，

都必须满足：

> **区域整体宽度保持不变。**

> **区域整体高度保持不变。**

例如：

```
日期
```

可以变成：

```
月｜日
```

要求：

```
月宽 + 日宽 = 原日期宽
```

不得影响：

摘要、

金额、

余额。

同理：

金额位数增加。

应重新分配金额区域。

而不是挤压其他区域。

---

# 四、布局计算原则

## 4.1 布局统一计算

页面中的：

- 行号
- 列号
- 行高
- 列宽
- 合并区域
- 偏移量

全部来源于：

```
ComputeLayout()
```

不得人工推导。

---

## 4.2 一个地方计算，全项目共享

布局信息只能存在于：

```
Layout
```

不得复制。

不得缓存多个版本。

不得重复计算。

---

## 4.3 固定坐标允许存在

模板最终定稿后：

固定坐标完全允许。

例如：

```
A3

F18

P26
```

这是最终模板。

允许。

但是：

这些坐标必须来源于：

```
Layout

↓

计算得到
```

而不是：

```
人工设计

↓

写死
```

即：

正确：

```
Layout

↓

A3
```

错误：

```
开发者：

"感觉这里应该是 A3"
```

---

# 五、演进原则

## 5.1 最小影响原则

任何布局修改，

影响范围必须最小。

例如：

```
日期
```

修改为：

```
月｜日
```

应只影响：

日期区域。

不得影响：

- 标题
- 摘要
- 金额
- 页尾

---

## 5.2 局部演进原则

任何业务需求，

必须首先定位：

属于哪个区域。

修改：

只能发生于：

该区域内部。

如果需要修改多个无关区域，

说明布局设计存在问题。

应先调整 LayoutSpec。

而不是继续修改 Renderer。

---

## 5.3 业务结构优先

布局变化，

应始终反映业务结构。

而不是 Excel 结构。

例如：

增加一个业务字段，

首先增加业务区域。

再计算布局。

而不是直接插入 Excel 列。

---

## 5.4 演进必须可持续

布局系统必须支持：

- 新增列
- 删除列
- 拆列
- 合列
- 增加金额位数
- 修改标题结构
- 修改展示方式

而不需要：

重新调整大量已有坐标。

任何需要大面积修改坐标的设计，

都属于布局设计缺陷。

---

# 六、Renderer 原则

Renderer 永远只负责：

```
Layout

↓

输出
```

不得：

- 推导布局
- 修正布局
- 自动调整位置
- 修改宽高

Renderer 不是布局系统。

---

# 七、AI Agent 行为规范

所有 AI Agent 在收到布局需求时，

必须遵循以下流程。

---

## Step 1

分析需求。

确定：

属于哪个业务区域。

禁止直接修改 Renderer。

---

## Step 2

优先调整：

LayoutSpec。

---

## Step 3

重新执行：

```
ComputeLayout()
```

生成新的：

```
Layout
```

---

## Step 4

检查影响范围。

确认：

是否仅影响目标区域。

若影响多个无关区域。

停止修改。

重新调整 LayoutSpec。

---

## Step 5

最后修改 Renderer。

Renderer 仅消费新的 Layout。

不得自行重新计算布局。

---

# 八、禁止事项

禁止：

- 人工维护大量坐标
- Renderer 自行计算布局
- Renderer 修正布局
- 一个区域依赖另一个区域的内部结构
- 因局部需求修改大量无关区域
- 未修改 LayoutSpec 就直接修改布局坐标
- 先修改 Excel，再反推布局模型

---

# 九、设计哲学

本项目始终坚持以下思想：

**页面布局以业务区域为基本单位，而不是以单元格为基本单位。**

**业务区域决定布局。**

**布局决定坐标。**

**坐标决定渲染。**

而不是：

**单元格决定页面。**

---

# 十、最终目标

开发阶段：

使用统一布局计算，

快速迭代模板，

避免任何局部修改导致整体返工。

模板定稿后：

自然收敛为固定坐标。

最终生成：

稳定、

一致、

可维护、

符合手工账格式的固定页面模板。

所有固定模板，

都必须能够追溯到统一的 LayoutSpec 与 ComputeLayout。

任何最终页面，

都应满足：

> **固定的是结果。**

> **可演进的是过程。**

布局计算服务于模板设计。

模板设计服务于最终交付。

这就是本项目页面布局系统的最高原则。