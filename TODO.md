# 待办事项

## 打印版区域样式配置（设计已确认，待实施）

见 `docs/print-styles-todo.md`：表头按区域（标题/科目/金额位数区/ML 明细区）与数据区按列（月日/字号/摘要/借或贷/金额区）的字体样式配置（字号/加粗/字体）。列宽行高不可配（平台补偿系数是核心）。当前版本仅实现"摘要/借/贷/余额表头 + 金额区域列"字体样式，其余待办。

## -f 级联重新生成

当前 `-f` 仅覆盖当月 xlsx，不自动级联更新后续月份。

修改 JSON 期初调整额后，需要从生效月起逐月手动重跑到最新月，否则后续月份的期初数据（来自旧 xlsx）会与新数据不一致。

待增强为：检测修改生效月 → 从该月起顺序重新生成所有后续月份。

## 战略补齐：面向「小微企业免代帐」目标（2026-08-31 评估结论）

1. **税务知识层**：技能新增 `references/tax.md`——税种日历、小规模纳税人/个体户申报口径、从账本数字到申报表各栏的填写指引、每月申报 checklist。补齐代帐公司核心交付，纯技能内容不改 CLI，优先级最高
2. **会计准则预设**：`init --preset 小企业会计准则`——科目模板 + 标准报表格式（当前内置 17 类为村集体会计制度科目，小微企业按小企业会计准则建账）**（2026-08-31 决策：暂缓——当前聚焦村/社区集体经济组织会计制度）**
3. **凭证输入 SOP**：技能固化「发票照片 → 凭证 md」「银行流水 → 日记账/对账」的标准流程模板

## 账套迁移：科目建立与期初余额导入（2026-08-31 设计定稿）

设计全文见 `docs/migration-design.md`（六阶段流程 / 三种迁移模式 / CSV 模板 / 双闸门验收 / git 纪律）。待实现：

- [ ] R1 `ledger subjects` 子命令组（list / export / import）：批量登记科目 + 设置科目属性——`SetAccountProperty` 目前有函数无 CLI 入口，类别指定只能直改 JSON
- [ ] R2 `ledger opening import -f 期初余额表.csv`：批量期初导入，写入时校验科目存在性 / 属性与方向一致 / 试算平衡（不平拒绝并列差额）；写入语义与 add-manual 一致（锚定建账月）
- [ ] R3 `init` 生成 `.gitignore`（派生产物：xlsx、ledger/balance 汇总、print/、closing/、output-harvest/；幂等补缺失）
- [ ] R4 迁移 SOP 沉淀进技能 `references/`（设计确认后实施；技能须自包含，不得引用仓库 docs/）

## JSON 字段核实结论（2026-08-31 全库核查）

- [ ] **`科目顺序` 未接线 → 已决策实施（2026-08-31）**：用户确认需要——自定义 Sheet 账页顺序，PDF 输出/打印装订按此序。实施要点：① `全局设置.科目顺序` 列总账科目名，决定 GL/ML/合并账页的 Sheet 顺序（未列出科目排其后，保持默认序）；② **活页打印页码连续逻辑必须联动**——页码跨 Sheet 连续编号，重排账页即需重编页码，这是本项的实现核心与风险点；③ `check.go:57` 提示文案同步修正；④ 走 openspec 变更流程实施
- [ ] **类别表硬编码 → 已决策搁置（2026-08-31）**：报表归类按总账科目名精确匹配内置 17 类（`balance.go` 的 `accountTypes`），JSON 无扩展类别字段。当前聚焦村/社区集体经济组织（17 类即为其科目体系），不做可配置类别表；check.go:57 文案中"或类别"字样待科目顺序实施时一并修正
- [x] 全字段文档：技能新增 `references/json-schema.md`（谁写谁读/字段状态/归类真相），README 样本补 `结账月`/`明细列顺序`
