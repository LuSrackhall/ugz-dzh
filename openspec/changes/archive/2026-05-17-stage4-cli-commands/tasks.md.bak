## 1. 依赖与骨架

- [ ] 1.1 引入 `github.com/spf13/cobra` 依赖
- [ ] 1.2 创建 `cmd/root.go`（根命令、全局 --version）
- [ ] 1.3 创建 `cmd/common.go`（共用函数：centsToYuan、cellName、writeCSV、writeXLSX 等从 main.go 迁移）
- [ ] 1.4 瘦身 `main.go` 为仅调用 `cmd.Execute()`

## 2. generate 子命令

- [ ] 2.1 创建 `cmd/generate.go`（迁移原 main.go 完整生成逻辑）
- [ ] 2.2 确认 generate 输出与重构前一致（CSV + XLSX + balance + 可选 workbook）

## 3. init 子命令

- [ ] 3.1 创建 `cmd/init.go`（生成初始 科目余额总览.json，-start-month 参数，拒绝覆盖）

## 4. check 子命令

- [ ] 4.1 创建 `cmd/check.go`（调用 balance.ValidateAccountTree，打印诊断）

## 5. add-manual 子命令

- [ ] 5.1 创建 `cmd/add_manual.go`（调用 balance.AddManualAdjustment，防重复）

## 6. reset 子命令

- [ ] 6.1 创建 `cmd/reset.go`（打开 xlsx 清除"需打印"标记）

## 7. year-close 子命令

- [ ] 7.1 创建 `cmd/year_close.go`（年末结转，生成新年度首月 xlsx）

## 8. 测试

- [ ] 8.1 编写 `cmd/` 包测试（generate 输出一致性、check 诊断、init 防覆盖、add-manual 防重复）
- [ ] 8.2 验证 `go test ./...` 全通过
- [ ] 8.3 手动验证 `ledger --help` 和 `ledger generate --help` 输出
