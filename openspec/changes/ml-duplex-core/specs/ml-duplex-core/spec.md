## ADDED Requirements

### Requirement: ML 数据行双区写入

系统 SHALL 确保多科目明细账的数据行同时写入 Back 区（7 基础列 + 明细 1~4）和 Front 区（明细 5~14）。

### Requirement: 纸1 正面空白占位

系统 SHALL 确保首次创建 ML Sheet 时，Front 区写入完整空白占位表（标题 4 行 + 列标题 1 行 + 20 行间距）。

### Requirement: 过次页双区写入

系统 SHALL 确保过次页/承前页行同时写入 Back 区和 Front 区。
