## 1. 包脚手架与数据结构

- [x] 1.1 创建 `generator/` 包，定义 `Workbook` 结构体（持有 excelize.File、config 引用、month 标识）
- [x] 1.2 定义 `SheetPage` 结构体（Sheet 名称、页码、数据行数）
- [x] 1.3 实现 `NewWorkbook` 构造函数（加载/复制上月 xlsx 或新建）
- [x] 1.4 实现上月期末提取函数 `extractLastMonthFinals`（遍历各 Sheet 过次页行 G 列值）

## 2. 总分类账 Sheet 基础

- [x] 2.1 实现 Sheet 命名规则：`总分类账-{科目全路径}`
- [x] 2.2 实现标题行、列标题行生成（日期/凭证号/摘要/借方/贷方/方向/余额）
- [x] 2.3 实现列宽和基础样式设置

## 3. 分录追加与分页

- [x] 3.1 实现 `AppendEntry` 方法（追加单笔分录到总分类账 Sheet）
- [x] 3.2 实现分页逻辑：每页 20 行数据 + 过次页行，满 20 行自动开新页
- [x] 3.3 实现过次页行的余额结转
- [x] 3.4 实现余额计算（首行 = 期初 + 借方 - 贷方，后续行 = 上行余额 + 借方 - 贷方）

## 4. 期初处理

- [x] 4.1 实现"上年结转"行（新科目且期初 != 0 时在首行插入）
- [x] 4.2 实现期初表 Sheet 生成（`{YYYY-MM}期初`）

## 5. 月结

- [x] 5.1 实现"本月合计"行（借方合计、贷方合计）
- [x] 5.2 实现"本年累计"行

## 6. 打印标记

- [x] 6.1 实现当月新写入行标记为"需打印"
- [x] 6.2 实现跨月未满页时的全页标记

## 7. 多科目明细账 Sheet

- [x] 7.1 实现多科目明细账 Sheet 创建（`多科目明细账-{总账科目}`）
- [x] 7.2 实现扩展列动态分配（H-U 对应各明细科目）
- [x] 7.3 实现父级汇总行（总账科目汇总其下所有明细）
- [x] 7.4 实现多科目明细账的分录追加与分页

## 8. 生成流程编排

- [x] 8.1 实现 `GenerateWorkbook` 入口函数（加载配置 → 解析凭证 → 复制/新建 → 提取期初 → 生成期初表 → 追加分录 → 月结 → 标记打印 → 回写余额 → 保存）
- [x] 8.2 首次生成时（无上月 xlsx）的初始化路径

## 9. main.go 集成

- [ ] 9.1 在 main.go 中添加 `-month` 和 `-json` 参数处理
- [ ] 9.2 调用 `generator.GenerateWorkbook` 生成 xlsx
- [ ] 9.3 确保 CSV 输出、balance CSV/XLSX 输出与 generator 输出共存

## 10. 测试

- [x] 10.1 编写 `generator` 包单元测试（核心函数：分页、余额计算、Sheet 命名）
- [x] 10.2 更新 e2e 测试，验证 xlsx 输出（Sheet 数量、标题、数据行数）
- [x] 10.3 编写多科目明细账测试（父级汇总行、扩展列）
